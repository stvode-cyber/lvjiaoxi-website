# 项目方案：公司官网 + 产品下载 + 后台数据管理

> 本文档是项目的完整需求与技术方案，重开任务时直接引用本文件（@ 引用）即可继续建设，无需重复背景。

## 一、项目背景与目标

做一个公司官网，核心要求：

1. **触达式动态**：页面元素滚动进入视口时才淡入 / 上移显现（scroll reveal），不是一打开就全显示。
2. **公司介绍** 与 **产品下载页** 两个核心页面。
3. 业务涵盖 **各种应用** 和 **游戏** 两类。
4. 带 **后台**：能看浏览量、下载量，能上架 / 下架产品。

## 二、已确认的技术决策

| 项 | 决策 |
| --- | --- |
| 前端 | 纯 HTML / CSS / JS，零构建，可直接静态托管 |
| 后端 | Node.js + Express + SQLite（文件数据库，免安装） |
| 后台鉴权 | 单管理员口令登录（Bearer Token） |
| 运行环境 | 本地 / 内网 |
| 内容 | 先用占位文案与 SVG 占位图搭框架，后续替换真实内容 |

## 三、整体架构

- **浏览器端**
  - 官网前端（index.html / download.html）：滚动触达动画 + 埋点上报
  - 后台（admin.html）：登录 + 数据看板 + 上架 / 下架产品
- **后端（Node + Express）**
  - 接收浏览 / 下载埋点，提供产品列表与后台管理接口
  - 数据存 SQLite（visits / downloads / products 三张表）

**数据流**
- 官网加载 / 点击下载 → 调 API 上报 → 写 SQLite
- 后台登录后 → 读 stats、增删改 products → 读 / 写 SQLite
- 下载页 → 拉取已上架 products → 渲染卡片

## 四、文件结构

```
项目根目录/
├── index.html              # 官网首页：导航 / Hero / 公司介绍 / 业务概览
├── download.html           # 产品下载页：运行时从 /api/products 拉卡片
├── admin.html              # 后台：登录 + 数据看板 + 上架 / 下架产品
├── assets/
│   ├── css/style.css       # 前端样式 + 滚动触达动画(.reveal / .in-view)
│   ├── js/main.js          # IntersectionObserver + 埋点上报 + 拉取产品
│   └── img/                # 占位图(SVG 色块)
├── server/
│   ├── server.js           # Express 入口：静态托管 + 接口路由
│   ├── db.js               # SQLite 初始化与建表 + 种子数据
│   ├── auth.js             # 后台口令校验(Bearer Token)
│   └── routes.js           # views / downloads / products / admin 接口
├── data/app.db             # SQLite 文件(首次运行自动生成)
├── package.json
└── README.md               # 启动步骤 + 内容替换说明
```

## 五、前端页面规划

### index.html（首页）
- 顶部导航：锚点跳转（首页 / 公司 / 应用 / 游戏 / 下载），平滑滚动
- Hero 首屏：公司名 + Slogan + 向下滚动提示，入场即触达动画
- 公司介绍：使命 / 愿景文案 + 实力数据（数字滚动到视口时跳动）+ 团队占位
- 业务概览：应用、游戏 两大入口卡片，点击跳 download.html

### download.html（产品下载页）
- 应用下载区：卡片网格（图标占位、名称、简介、下载按钮）
- 游戏下载区：卡片网格（封面占位、名称、简介、下载按钮）
- 卡片数据由 JS 从 `GET /api/products?type=app|game` 拉取
- 点下载按钮：先 `window.open(download_url)` 再 `POST /api/downloads` 上报

### admin.html（后台）
- 登录页：输入口令 → `POST /api/admin/login` 拿 Token（存 localStorage）
- 数据看板：总浏览量、各页浏览、各产品下载量（数字 + 简单条形）
- 产品管理：列表（名称 / 类型 / 状态）、新增（上架）、编辑、上下架切换、删除

## 六、后端 API

**前台埋点（无需登录）**
- `POST /api/views` — 页面加载上报浏览，body：`{ "page": "home|download" }`
- `POST /api/downloads` — 点下载按钮上报，body：`{ "productId": 1 }`
- `GET /api/products?type=app|game` — 下载页拉取「已上架(on)」产品

**后台（口令登录后可用，带 Authorization: Bearer <token>）**
- `POST /api/admin/login` — body：`{ "password": "..." }`，返回 `{ token }`
- `GET /api/admin/stats` — 看板数据：总浏览量、各页浏览、各产品下载量
- `GET /api/admin/products` — 全部产品（含已下架）
- `POST /api/admin/products` — 上架新产品，body：`{ name, type, desc, icon, download_url }`
- `PUT /api/admin/products/:id` — 编辑或上下架（status: on|off）
- `DELETE /api/admin/products/:id` — 删除

## 七、数据表（SQLite）

- `visits(id, page, ip, ts)`
- `downloads(id, product_id, ts)`
- `products(id, name, type[app|game], desc, icon, download_url, status[on|off], created_at)`

种子数据建议（占位）：
- 应用：效率笔记、团队协作、云盘同步
- 游戏：星际探险、消除乐园、赛车竞速

## 八、触达式动态实现要点

- CSS：元素默认 `opacity:0; transform: translateY(40px); transition: .6s ease`
- `.in-view` 类：`opacity:1; transform:none`
- JS（main.js）：`IntersectionObserver` 扫描 `[data-reveal]` 元素，进入视口（threshold≈0.15）加 `.in-view`；同区块子元素用 `transition-delay` 做 stagger 依次浮现
- 首屏 Hero 用同样机制或 CSS 入场动画
- 尊重 `prefers-reduced-motion`：关闭动画时直接显示

## 九、视觉规范（建议，可改）

- 科技蓝紫调：深蓝底 + 紫 / 青点缀，契合应用与游戏气质
- CSS 变量统一管理配色 / 间距 / 圆角 / 阴影，换肤只改一处
- 移动端单列网格自适应，动画在手机上同样生效

## 十、启动与部署

1. `npm install`
2. `node server/server.js`
3. 访问 `http://localhost:3000`（官网）、`http://localhost:3000/admin.html`（后台）
4. 后台默认口令：`admin123`（建议改为自定义，写在配置中）

## 十一、需用户后续替换的内容清单

- 公司名、Slogan、Logo、真实文案
- 产品清单（名称 / 类型 / 简介 / 图标 / 下载链接）
- 配色偏好
- 后台口令

## 十二、建议的建造步骤（供重开任务执行）

1. 初始化 `package.json` + 安装 `express`、`better-sqlite3`
2. 写 `server/db.js`（建表 + 种子数据）
3. 写 `server/auth.js`（口令校验 + Bearer Token）
4. 写 `server/routes.js`（views / downloads / products / admin 接口）
5. 写 `server/server.js`（静态托管 + 路由挂载）
6. 写 `assets/css/style.css`（布局 + 触达动画）
7. 写 `assets/js/main.js`（IntersectionObserver + 埋点 + 拉取产品）
8. 写 `index.html` / `download.html` / `admin.html`
9. 本地启动自测：滚动动画、浏览 / 下载上报、后台登录与上下架
