# 2026-08-31 工作日志

## 导航条升级（全权开发）
- CSS `style.css`：`.nav` 增加 `.scrolled` 玻璃态强化（滚动后加深背景+阴影）；新增 `.hamburger` 汉堡按钮与移动端下拉面板（`@media max-width:720px` 改为可展开菜单，取代原直接隐藏链接）；导航项 `.active` 高亮（下划线）；`section[id]/header[id]` 加 `scroll-margin-top` 补偿吸顶；新增 `.biz-wrap` 居中单卡。
- `index.html`：导航加汉堡按钮；品牌改为 `<a href="#home">` 可点回顶；原「两大业务线」单区块拆分为 `#apps` 与 `#games` 两个独立锚点精选区块（修复原「游戏」导航无对应锚点）。
- `download.html`：导航加汉堡按钮；品牌改为 `<a href="index.html">` 回官网。
- `assets/js/main.js`：新增导航增强段——滚动 `.scrolled` 切换、汉堡开合（`nav-open`，点击链接/外部关闭、移动端锁滚动）、IntersectionObserver scrollspy 高亮当前章节对应导航项。
- 验证：node --check 通过；:3000 静态资源实时生效（旧进程直接读盘，无需重启）。

## 下载页增强（搜索 + 分类筛选 + 空/暂缺态）
- `download.html`：Hero 后新增筛选栏（搜索框 `#search-input` + 分类 chips `#type-chips`：全部/应用/游戏）。
- `assets/js/main.js`：下载段重构——一次拉取全量已上架（`GET /api/products` 无 type 返回全部 on），前端按 `state{type,kw}` 本地过滤并分桶渲染到 `#app-list`/`#game-list`；搜索匹配 name+desc，分类切换显示/隐藏对应 section；导航「应用/游戏」联动分类筛选；下载卡片**去掉 `.reveal` 类**（修复动态插入卡片不被 IntersectionObserver 观察导致透明不可见的隐藏 bug）；无下载链接（url 空或 `#`）时按钮渲染为禁用态「敬请期待」，点击监听加 `btn.disabled` 拦截。
- `assets/css/style.css`：新增 `.filters`/`.search-box`/`.chips`/`.chip(.active)` 与 `.download-btn.is-disabled` 样式。
- 服务因旧进程已退出，已用最新代码重启（PID 973，启动打印 config 安全提醒），静态与 server 均为最新；验证 / 与 download 200、8 款产品、新元素全部命中。
- 注：种子产品 download_url 仍为占位 `#`，故下载页目前均显示「敬请期待」；后台填入真实链接即变可下载。

## 后台管理增强（可视化看板 + 产品搜索筛选）
- `server/routes.js`：`/admin/stats` 新增 `daily`（近 14 天本地日期分桶的浏览/下载量，新增 `bucketDaily()` 模块函数，按 ts 毫秒分桶）+ `topProducts`（按下载量降序 TOP8，含 id/name/type/status/c）。
- `admin.html`：原「各产品下载量」面板替换为「流量趋势（近14天）」canvas 面板（含图例）+「热门下载 TOP」排行面板（`#top-list`）；产品管理区加工具栏 `#pm-search` 搜索框 + `#pm-filter` 分类 chips（全部/应用/游戏）。
- `assets/js/admin.js`：重写——新增 `drawTrend()` 纯 Canvas 双线图（紫=浏览/青=下载，含网格、Y 轴刻度、渐变面积、数据点、X 轴日期标签，devicePixelRatio 适配，窗口 resize 重绘）；`rankList()` 渲染排行（前三奖牌）；产品列表重构为 `allProducts` + `renderProducts()`，按搜索词 + 类型客户端过滤。
- `assets/css/style.css`：新增 `.chart-head`/`.chart-legend`/`.trend-canvas`、`.rank-list`/`.rank-item`/`.rank-no`/`.rank-tag`(app/game)/`.rank-val`、`.pm-toolbar` 样式。
- 重启前 :3000 被旧进程(PID 14824)占用，`pkill` 无效，改用 PowerShell `Stop-Process -Id 14824 -Force` 释放后重启成功（PID 1268，新 backend 生效）。验证：routes/admin.js 语法 OK；stats 含 daily(14天)/topProducts；admin.html 含 trend-chart/top-list/pm-search/pm-filter；首页/admin 200。

## 下载页产品详情弹窗（列表→详情→下载闭环）
- 背景：下载页此前只能列表+下载，缺详情。本次补全"列表→详情→下载"闭环。
- `download.html`：body 末新增产品详情弹窗 `#pd-modal`（backdrop + `.modal-card` + 关闭× + `#pd-icon`/`#pd-name`/`#pd-type`/`#pd-desc`/`#pd-download`/`#pd-hint`，accessibility：`role=dialog`、`aria-modal/aria-hidden`、关闭用 `[data-close]`）。
- `assets/js/main.js`：卡片加 `data-id`；`loadAll` 拉全量后构建 `byId` 索引；卡片底部 `.pcard-foot` 含下载按钮 + 「详情 ›」按钮；新增 `openModal(p)`/`closeModal()`——填充图标/名称/类型标签(app/game 配色)/简介/下载按钮(有链接可点、无链接禁用+提示"资源筹备中")；点击卡片(非下载按钮)开弹窗，点击 backdrop/×/ESC 关闭，弹窗内下载后自动关闭；`byId` 快速取产品避免二次请求。
- `assets/css/style.css`：`.product-card{cursor:pointer}` + `.pcard-foot` 布局 + `.detail-btn`；弹窗全套 `.modal`/`.modal-backdrop`/`.modal-card(.show 缩放淡入)`/`.modal-x`/`.modal-icon`/`.modal-meta`/`.modal-type(app/game)`/`.modal-desc`/`.modal-actions`/`.modal-hint` 及 `body.modal-open{overflow:hidden}` 锁滚。
- 验证：main.js 语法 OK；`:3000` 静态实时生效（无需重启），pd-modal/pd-download/openModal/closeModal/byId 及弹窗样式全部命中；首页/下载页 200。

## SEO 与社交分享基础（production 必备）
- 背景：站点功能完整后，补齐"可被搜索引擎收录 + 社交分享有卡片"的标准组件。
- 三页 head 增强：`index.html`/`download.html`/`admin.html` 均加 `<link rel="icon" href="assets/img/logo.svg">`、`<meta name="theme-color" content="#0a0e27">`；首页/下载加 `description`/`keywords`/`canonical` + Open Graph(`og:type/site_name/title/description/url/image`) + Twitter Card(`summary_large_image` + title/description/image)；后台加 `<meta name="robots" content="noindex, nofollow">` 不被收录。
- 所有 OG/Twitter 域名用占位 `https://lvjiaoxi.example.com/`（上线前需替换为真实域名）。
- `robots.txt`（根目录）：允许全站抓取，`Disallow: /admin.html`，指向 Sitemap。
- `sitemap.xml`（根目录）：列出首页 + 下载页 2 条（后台 noindex 不收录）。
- `assets/img/og.png`：用 ImageGen 生成 1200×630 品牌社交卡（提示词：深蓝底+紫青渐变光晕+犀牛图标+「绿角犀/LVJIAOXI」字样；生成尺寸 1536×1024 横图，环境无 ImageMagick/ffmpeg 无法精确裁剪，故直接以 og.png 命名使用，比例非严格 1.91:1，后续可替换为精确 1200×630）。
- 验证：`:3000` 静态实时生效，robots.txt(200 text/plain)/sitemap.xml(200 application/xml, 2 条 loc)/og.png(200 image/png)/logo.svg(200) 全部可访问；首页+下载页 og:title/twitter:card/favicon/og:image 全部命中；admin noindex 命中。

## 产品详情独立页（可寻址 + 可分享 + 可收录）
- 背景：SEO 基础只做了"站点级"分享图；要让每个产品也能被独立分享/收录，需可寻址详情页（URL 带 ?id=）并带各自 OG meta。此步是 SEO 的自然延续。
- `server/routes.js`：新增 `GET /api/products/:id`（在 `/products` 之后），返回单个**已上架**产品必要字段；缺失或 `status!='on'` 返回 404；非法 id 返回 400。
- `product.html`（新）：独立详情页，nav + 详情卡（大图标/名称/类型标签/简介/下载按钮/返回下载中心）+ "更多产品" 区 + 页脚；含完整 OG/Twitter meta 占位（JS 动态覆盖）。`data-page="product"`。
- `assets/js/product.js`（新）：读取 URL `?id=` → `GET /api/products/:id` 渲染详情；**动态注入** document.title / description / og:title / og:description / og:url / twitter:* / canonical（使每产品独立 shareable）；下载按钮（有链接打开+上报，无链接禁用）；"更多产品"拉 `/api/products` 取其余 4 个并链接到各自详情页；浏览上报 `page:'product:'+id`；404→"产品不存在或已下架"友好页；含 nav 强化(`.scrolled`/汉堡)与 `.reveal` 动画（与 main.js 一致，独立页自包含）。
- `assets/css/style.css`：新增 `.pd-hero/.pd-loading/.pd-card/.pd-icon/.pd-main/.pd-title/.pd-type(app/game)/.pd-desc/.pd-actions/.pd-note/.pd-err/.pd-more-wrap` 详情页样式，以及 `.modal-page-link`（弹窗"查看完整页面"链接样式）。
- `download.html`：弹窗 `.modal-actions` 内新增 `<a id="pd-page" href="#">查看完整页面 ›</a>`。
- `assets/js/main.js`：`openModal` 中设置 `pd-page` 的 href 为 `product.html?id=<id>`，使弹窗与独立页互通。
- 服务进程管理教训：此前用 `node ... &` 在 Bash 工具内启动，进程会随该工具调用的 shell 退出被回收（端口从 LISTENING 变 TIME_WAIT）。本次改用 **Bash `run_in_background:true` 后台托管**启动，PID 16816 持续监听 3000，预览稳定。
- 验证：routes.js / product.js `node --check` 通过；重启后 首页/下载页/详情页/product.js 均 200；`/api/products/16`→200(含产品JSON)，`/api/products/1`→404(该id早已在早前重置时删除，自增使真实id为16-23)；全量已上架 8 款。终端里产品名显示为乱码系 Windows 控制台 UTF-8 显示问题，DB 与浏览器渲染均为正常中文。

## 动态 sitemap + downloads 目录（SEO 闭环 + 安装包投放）
- 背景：静态 sitemap.xml 只列首页+下载页，无法随后台产品变动自动收录产品详情页；且下载按钮因 download_url 占位 `#` 均不可点。本步让 sitemap 动态化（自动收录在架产品），并打通安装包目录。
- `server/config.js`：新增 `siteUrl`（默认占位 `https://lvjiaoxi.example.com`，支持 `SITE_URL` 环境变量覆盖），供 sitemap/robots 使用。
- `server/server.js`：重写为在**静态托管前**挂载两个路由——`GET /robots.txt`（动态，Disallow `/admin.html` 与 `/api/`，Sitemap 指向 `${SITE}/sitemap.xml`）、`GET /sitemap.xml`（动态 `buildSitemap()`：收录首页 daily/1.0 + 下载页 daily/0.8 + 全部 `status='on'` 产品 `product.html?id=<id>` weekly/0.6，XML 用 `escapeXml` 转义）；新增 `/downloads` 静态挂载（真实安装包投放处）；新增 `buildSitemap()`/`escapeXml()` 模块函数。
- 删除根目录旧的静态 `robots.txt` 与 `sitemap.xml`（避免被同名静态文件覆盖动态路由），改由路由动态生成。
- 新建 `downloads/` 目录 + `downloads/README.md`（说明安装包投放位置与后台填 `/downloads/xxx` 链接后即生效）。

## 后台控制台收尾（刷新按钮 + 下载量列）
- 背景：审查发现后台功能其实已较完整（编辑/上下架/删除/搜索/筛选/趋势图俱备），缺的是「手动刷新」与「产品下载量直观展示」。
- `server/routes.js`：`GET /admin/products` 改为 LEFT JOIN downloads 返回 `downloads` 计数字段（原 `SELECT *`），前端表格可直接展示每产品累计下载量。
- `admin.html`：`.admin-head` 加「↻ 刷新数据」按钮 `#refresh-btn` 与退出并列；KPI 上方加 `#updated-at`（最后更新时间）；产品表头加「下载量」列。
- `assets/js/admin.js`：把 `loadStats` 的渲染体抽成 `applyStats(s)`（登录/自动加载与刷新共用）；新增刷新按钮监听——并行重载 stats+products，`↻ 刷新中…` 禁用态防重复点击，完成后复原；`nowStr()` 生成 HH:MM:SS 写入 `#updated-at`；`renderProducts` 表格行加 `.dl-count` 下载量单元格。
- `assets/css/style.css`：`.head-actions`/`.refresh-btn(:disabled)`/`.updated-at`/`.table .dl-count`（右对齐+青色+等宽数字）样式。
- 重启（PowerShell `Stop-Process -Id 6968 -Force` 释放 3000 → Bash 后台托管重启，PID 2820 持续监听）。验证：routes/admin.js 语法 OK；`/api/admin/products` 返回含 `downloads` 字段（种子产品均为 0）；admin.html 含 refresh-btn/updated-at/下载量表头；CSS 三规则 + JS applyStats/refresh/dlcound 全部命中。
- 重启（PowerShell `Stop-Process -Id 16816 -Force` 释放 3000 → Bash 后台托管重启，PID 6968 持续监听）。验证：首页/下载页/详情页 200；`/robots.txt` 文本正确；`/sitemap.xml` 含首页+下载页+8 款产品详情页共 10 条 loc；`/downloads/README.md` 200(text/markdown)。
- 注：OG/Twitter meta 的域名仍是 HTML 内硬编码占位 `https://lvjiaoxi.example.com/`，上线前需与 config.siteUrl 一并替换为真实域名；产品 download_url 仍为占位 `#`，放入真实安装包并填链接后下载按钮即变可点。

## 安全加固 + 友好 404（production 收尾）
- 背景：功能与运营面已完整，本轮做上线前的基础安全与健壮性收尾，均零依赖、无需素材。
- `server/server.js`：
  - `app.disable('x-powered-by')` 隐藏技术栈；
  - 全局中间件注入基础安全头：`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`（防被 iframe 嵌套）、`Referrer-Policy: no-referrer-when-downgrade`、`X-XSS-Protection: 1; mode=block`；
  - 新增 `GET /favicon.ico` 路由直接返回 `assets/img/logo.svg`（避免浏览器默认请求 404）；
  - 末尾加 404 兜底中间件：非 `/api/` 路径返回 `404.html`（status 404 + text/html），`/api/` 仍返回 JSON 404。
- 新建 `404.html`：沿用站点视觉（导航 + 大号渐变 404 + “这个页面走丢了”文案 + 返回首页按钮），含 `noindex`。
- 重启（PowerShell `Stop-Process -Id 2820 -Force` 释放 3000 → Bash 后台托管重启，PID 8384 持续监听）。验证：首页响应头含 4 个安全头且无 X-Powered-By；首页/下载页 200；`/favicon.ico`→200(image/svg+xml)；未知路径→404(text/html)且含友好文案；`/api/whatever`→404(application/json)。

## 联系我们页 + 留言后台（inbound 闭环）
- 背景：企业站缺 inbound 渠道。本步加「联系我们」页（表单→后端入库）+ 后台留言看板，形成「访客留言→后台可见→可删除」闭环；占位联系方式待上线替换。
- `server/db.js`：新增 `contacts` 表（id/name/email/message/ts）。
- `server/routes.js`：公开 `POST /contact`（校验 name+message 必填、email 格式，入库）；`GET /admin/stats` 新增 `totalContacts`；新增 `GET /admin/contacts`（鉴权，按 ts 倒序）与 `DELETE /admin/contacts/:id`（鉴权）。
- `contact.html`（新）+ `assets/js/contact.js`（新）：导航(含 active 高亮) + 头部 + 左侧占位联系方式(aside) + 右侧表单（name/email/message，前端校验+提交+成功/失败态，复用 main.js 的导航交互）；含完整 OG/Twitter meta；页脚回首页/后台。
- 导航接入：index.html 导航与页脚加「联系」链接；download.html 导航加「联系」链接。
- `admin.html`：产品管理前新增「联系留言」面板 `#contact-list`；KPI 增加「联系留言」项。
- `assets/js/admin.js`：新增 `loadContacts()`（渲染最近 20 条，含时间/邮箱/内容/删除按钮），`showApp` 与刷新按钮均调用；`#contact-list` 事件委托删除（confirm 后重载）。
- `assets/css/style.css`：`.subhero`/`.contact-wrap`/`.contact-aside`/`.contact-list`/`.contact-form`/`.form-msg(.ok/.err)`/`.msg-list`/`.msg-item`(+top/name/time/email/text) 及移动端单列。
- 重启（PowerShell `Stop-Process -Id 8384 -Force` → Bash 后台托管重启，PID 16676 持续监听）。验证：首页/下载/联系/contact.js 200；`POST /contact`→ok，缺 name→400；stats.totalContacts 计数正确、/admin/contacts 可见；首页+下载导航含 contact.html 链接；联系页表单+脚本就位。测试留言已删除，后台归零。

## 首页实时产品展示（catalog 联动）
- 背景：首页此前只有静态业务介绍卡片，未展示真实在架产品。本步让首页从 `/api/products` 实时拉取在架产品，按应用/游戏分桶展示在对应业务线下，卡片链接到产品详情页——首页与产品目录打通（无需素材）。
- `index.html`：`#apps` 与 `#games` 业务区各加 `<div class="product-grid" id="home-apps">` / `#home-games`，位于介绍卡片 `.biz-wrap` 之后。
- `assets/js/main.js`：新增 `homeCardHTML(p)`（图标/名称/类型/简介 + “查看详情 ›” 链接到 `product.html?id=<id>`，卡片带 `data-home="1"`）+ `loadHomeProducts()`（拉全量在架、按 type 分桶渲染两个网格，空/失败态提示）；启动处 `loadAll()` 后调用 `loadHomeProducts()`；修改全局卡片点击处理——`data-home` 卡片跳过 modal（走详情页链接）。`esc` 为函数声明已 hoist可用。
- `assets/css/style.css`：`.product-grid` 加 `margin-top:26px` 与首页分隔；新增 `.product-card[data-home]{cursor:default}`（首页卡片靠内部链接导航，不弹 modal）。
- 仅前端改动，静态文件由运行服务(PID 16676)实时读盘，无需重启。验证：main.js 语法 OK；首页含 #home-apps/#home-games 容器；main.js 含 loadHomeProducts/data-home 逻辑；CSS 含 data-home 样式；`/api/products?type=app|game` 各返回 4 款；首页 200。

## 后台增强：自动刷新 + 留言 CSV 导出（运营收尾）
- 背景：后台已具刷新/看板/上下架/留言功能，缺「常驻自动刷新」与「留言可归档导出」。本步补齐两项零依赖运营能力（无需素材）。
- `admin.html`：`.head-actions` 新增 `#auto-btn`（自动刷新开关，默认关）；联系留言面板改 `.panel-head`（标题 + `#export-csv` 导出按钮）。
- `assets/js/admin.js`：手动刷新重构为 `doRefresh(silent)`（silent 时不改按钮文案、不受手动禁用态影响）；新增 `startAuto()/stopAuto()` 以 `setInterval` 每 30s 调用 `doRefresh(true)`，开关态存 `localStorage(AUTO_KEY)` 记忆；`exportCsv()` 拉全量 `/admin/contacts`，用 `csvCell()` 做引号/逗号/换行转义，生成带 BOM(`﻿`) 的 UTF-8 CSV 并经 Blob 触发下载（文件名 `绿角犀留言_YYYYMMDD-HHMM.csv`），空数据时提示「暂无留言可导出」，导出中禁用防重复。
- `assets/css/style.css`：新增 `.panel-head`（flex 两端对齐）/`.refresh-btn.on`（青色高亮 + 光晕，标识自动刷新开启）。
- 服务(PID 16676)静态实时读盘，无需重启。验证：admin.js `node --check` 通过；已服务 HTML 含 auto-btn/export-csv/panel-head（5026B，+310B）、JS 含 doRefresh/startAuto/stopAuto/exportCsv/csvCell/AUTO_KEY/REFRESH_MS；登录拿 token→`/admin/contacts` 返回 ts 为毫秒数、中文原文正常；插入含逗号/引号/换行的测试留言→导出链路字段正确→删除归零（终端中文显示乱码系 Windows 控制台 UTF-8 怪象，DB/浏览器正常）。

## 后台增强：产品目录 CSV 导出（数据归档闭环）
- 背景：上轮已补「留言 CSV 导出」，本步补齐对等能力——产品目录也可一键导出归档，形成后台核心数据（留言 + 产品）双导出闭环。无需素材、零依赖。
- `admin.html`：产品管理面板改为 `.panel-head`（标题 + `#export-products`「⬇ 导出 CSV」按钮），复用已有 `.panel-head`/`.refresh-btn` 样式。
- `assets/js/admin.js`：新增 `exportProducts()`——直接读取内存全量 `allProducts`（无需额外请求），用既有 `csvCell()` 转义，导出列：ID/名称/类型/状态/下载量/下载链接/简介；带 BOM 的 UTF-8 CSV 经 Blob 下载（文件名 `绿角犀产品_YYYYMMDD-HHMM.csv`），空数据提示防误触。
- 服务静态实时读盘，无需重启。验证：admin.js `node --check` 通过；已服务 HTML 含 export-products、JS 含 exportProducts(函数+监听)；`/api/admin/products` 返回字段含 id/name/type/status/downloads/download_url/desc（共 8 款），导出所需字段齐全。

## 后台增强：留言搜索 + 分页（应对留言量增长）
- 背景：联系留言面板此前只渲染前 20 条且无检索，量大会不够用。本步加搜索（姓名/邮箱/留言）+ 分页（每页 10），与产品管理一致采用"全量拉取→前端过滤+分页"，零新接口、无需素材。
- `admin.html`：联系留言面板在 `.panel-head` 下加 `.pm-toolbar`（搜索框 `#contact-search`）+ `#contact-list` + 分页容器 `#contact-pager`。
- `assets/js/admin.js`：`loadContacts()` 改为存全量 `allContacts` 后调 `renderContacts()`；新增 `filteredContacts()`（按关键词过滤 name+email+message）、`fmtTime()`、`renderContacts()`（过滤+分页，空态区分"暂无/无匹配"、底部 `#contact-pager` 渲染上一页/页码信息/下一页）、`#contact-search` input 与 `#contact-pager` 点击（prev/next）监听；删除逻辑无需改（`data-del` 按钮不变）。导出仍走全量 `/admin/contacts`，不受过滤影响。
- `assets/css/style.css`：新增 `.pager`/`.pager .pg`/`.pager .pg:disabled`/`.pager .pg-info` 分页器样式。
- 服务静态实时读盘，无需重启。验证：admin.js `node --check` 通过；已服务 HTML 含 contact-search/contact-pager、JS 含 renderContacts/filteredContacts/CONTACT_PAGE_SIZE；算法经纯内存数组（原生 UTF-8 中文）验证：13 条→2 页(10+3)，搜索 张三/用户5/分页/定价/不存在xyz 命中分别为 1/1/13/1/0，第2页=3 全部正确（注：经 curl 落盘文件再读回的测试会因 Windows 控制台 UTF-8 编码污染而误判命中 0，非前端 bug；浏览器内 JSON 为干净 UTF-8，匹配正常）；测试留言已全部清理。

## 后台增强：修改口令（账户安全闭环，上线收尾）
- 背景：此前后台口令硬编码在 config.adminPassword，改口令需改代码/重启；且旧 auth.js 的 Token 在模块加载时按初始口令**固定派生**，口令变更无法使旧 Token 失效。本步把口令改为「DB 持久化 + Token 动态派生」，并给后台加可视化改口令面板（无需素材、零依赖）。
- `server/db.js`：新增 `config` require；建 `settings(key,value)` 表；导出前 `INSERT OR IGNORE` 种子 `admin_password=config.adminPassword`（重启不覆盖已修改值）。
- `server/auth.js`：重写为动态模型——`getAdminPassword()` 从 settings 表读（回退 config 默认值）；`makeToken(pw)` 按"当前口令"派生；`login(pw)`/`verifyToken(tok)` 均基于**最新口令**派生比较 → 口令变更后旧 Token 立即失效；导出 `login/verifyToken/makeToken/currentToken/getAdminPassword`。
- `server/routes.js`：require 增 `makeToken,getAdminPassword`；登录返回 `makeToken(password)`（弃用固定 TOKEN）；新增 `PUT /api/admin/password`（authMiddleware）：校验 oldPassword 正确 + newPassword≥6 位 → `INSERT OR REPLACE settings.admin_password` → 返回新 token。
- `admin.html`：产品管理面板后新增「账户安全」面板（`#pw-form` 含 old-pw/new-pw/new-pw2/pw-submit + `#pw-msg` 提示，复用 `.field`/`.form-msg`/`.btn-primary`）。
- `assets/js/admin.js`：新增改口令 submit 监听——前端校验（必填/≥6位/两次一致）→ `PUT /api/admin/password` → 成功后用返回新 token 覆盖本地 `localStorage`（后续请求自动生效）+ 成功提示并清空表单；失败给错误提示。
- `assets/css/style.css`：新增 `.pw-form`(max-width:440px) 与微调。
- 重启（PowerShell `Stop-Process -Id 16676 -Force` → Bash 后台托管重启，task y8WHGZ 持续监听）。验证：`node --check` 4 文件全过；完整链路实测——登录 admin123→T1；改口令→T2；**旧 T1 访问 /admin/stats=401（失效）**、新 T2=200；admin123 再登录=401（口令已改）、test123 登录=200；新口令<6位=400；还原 admin123 后登录=200。环境已还原为 admin123。三文件钩子全部命中。

## 域名统一收口（单一配置源 + 服务端 HTML 域名替换中间件，上线收尾）
- 背景：上线前需把各 HTML 硬编码的占位域名 `https://lvjiaoxi.example.com`（canonical/og:url/og:image/twitter:image 共 ~18 处）替换为真实域名。机械逐页改字符串易遗漏且对 SEO 爬虫不友好（OG 必须服务端静态正确）。本步把域名收口为**单一配置源（config.siteUrl / 环境变量 SITE_URL）**，并由服务端在响应 HTML 时统一替换，上线只需改一处。
- `server/server.js`：加 `fs` require；新增 `const PLACEHOLDER='https://lvjiaoxi.example.com'` 与 `GET /api/site`（返回 `{siteUrl:SITE, siteName:'绿角犀'}`，作为前端统一配置点）；新增 HTML 域名替换中间件（注册在 `express.static` 之前）——对 GET `/` 或 `*.html/*.htm` 用 `fs.readFile` 读取后以 `split(PLACEHOLDER).join(SITE)` 替换占位域名再 `res.send`（对 SEO 友好、无需 JS）；文件不存在则 `next()` 交 404 兜底（状态码保持 404）。
- `assets/js/product.js`：`var BASE` 由硬编码 `'https://lvjiaoxi.example.com'` 改为 `location.origin`（运行时使用真实访问域名，去掉前端硬编码域名；product.html 静态 og:image 仍由服务端中间件替换为 SITE）。
- 说明：contact.html 的占位邮箱 `bd@/press@lvjiaoxi.example.com` 不含 `https://` 前缀，不匹配 PLACEHOLDER，不会被误替换；三页 HTML 自身仍保留占位作兜底，由中间件运行时替换。
- 验证：`node --check` server.js/product.js 全过；用 `SITE_URL=https://www.lvjiaoxi.com` 启临时实例实测——index/download/contact/product.html 的 og:image/og:url 全部变为 www.lvjiaoxi.com、`/api/site` 返回该域名、product.js BASE=location.origin、contact 邮箱未误替换；停临时实例后用默认配置重启（task vgxcaT 持续监听），确认默认 og:image 仍 example.com、`/api/site` 返回 example.com、首页 200，行为不变。临时文件已清理。

## 后台登录防爆破（账户安全闭环收尾）
- 背景：此前 `/api/admin/login` 无任何失败限制，存在暴力破解风险。本步补齐账户安全最后一环（前已做安全响应头/404/修改口令），纯代码零依赖无需素材。
- `server/routes.js`：登录接口加进程内防爆破——`loginAttempts` Map 按 `req.ip` 记录失败次数；连续失败达 `MAX_LOGIN_FAILS=5` 次即锁定 `LOGIN_LOCK_MS=15分钟`；锁定期间登录返回 `429` 并提示剩余秒数；登录成功则清零该 IP 计数。`getLoginLock(ip)` 处理锁定查询与过期清理。仅挡登录入口，不影响已认证的 Bearer Token 请求。
- 前端无需改动：登录页错误提示复用既有 `res.j.error`（429 也走 `!res.ok` 分支，直接展示"尝试次数过多，请 X 秒后再试"）。
- 重启（PowerShell `Stop-Process` 释放 3000 → Bash 后台托管重启，task 3nd2hH 持续监听）。验证：`node --check` routes.js 通过；实测——正确口令登录拿 token(200)；错误口令连试 5 次全 401；第 6 次登录 429（锁定）；**锁定期间已登录 token 访问 /admin/stats 仍 200**（锁定只挡登录）；锁定期间正确口令登录也 429；测试后重启重置内存计数，admin123 登录恢复 200、首页 200。临时文件已清理。

## 上线前收尾：.env 真实化 + 进程健壮性 + 部署文档（维度22-26）
- `server/env.js`（新增）：零依赖 .env 加载器，读取项目根 `.env` 注入 `process.env`（支持 # 注释、KEY=VALUE、引号包裹；不覆盖已有变量）。初版仅 `module.exports = loadEnv` 未自动执行，导致 `require('./env')` 不读文件、SITE_URL 不生效；已修复为末尾 `loadEnv()` 模块加载即执行。
- `server/server.js`：顶部最优先 `require('./env')`（早于 `require('./config')`）；底部 `const server = app.listen(...)` + 优雅关闭（SIGTERM/SIGINT → `server.close` → `db.close` → exit，含 10s 强杀兜底）+ 全局异常兜底（uncaughtException/unhandledRejection 仅记录不崩溃）。
- `DEPLOY.md`（新增，中文部署手册）：环境要求、npm 启动、PM2/systemd/nginx+HTTPS、SITE_URL、备份恢复、升级、健康检查、故障排查。
- `README.md`：补充 .env 生效说明 + 部署入口链接 + SITE_URL/OG图/downloads投放/健康检查段落。
- `downloads/README.md`：安装包投放方式说明（手动放入 / 后台填外链）。
- `.env.example`：去掉"未引入 dotenv"警告（现为内置零依赖加载器读取）。
- 验证：dotenv 生效（临时 `.env` 设 `SITE_URL=https://dotenv-test.example.com` → `/api/site`、sitemap、index canonical 全部替换成功）；清理 `.env` 后恢复默认；全部页面/端点 200。服务以 task I6wnz8 在 3000 持续监听。
- 已知限制：Windows 沙箱无 POSIX 信号，`SIGTERM` 优雅关闭无法在此验证，生产 Linux（PM2/systemd）环境生效。
- 踩坑（再记）：Bash 中 `cd /c/Users/Administrator\Desktop/绿角犀官网`（反斜杠）会被吞成 `/c/Users/AdministratorDesktop/绿角犀官网` → No such file or directory，必须用正斜杠。

## 品牌素材 AI 生成（维度27：占位视觉升级）
- 用内置 ImageGen 生成两张品牌占位素材（每张约 5-10 credits，共 2 张）：
  - `assets/img/og.png`（1200×624，社交分享主图，替换原占位 PNG；标准 OG 推荐 1200×630，差 6px 不影响展示）。
  - `assets/img/icon-512.png`（1024×1024，方形品牌图标，PWA/社交 avatar 用）。
- `manifest.json`：icons 数组新增 `icon-512.png`（type image/png, sizes any），与 logo.svg 并存。
- 未改动产品卡片 emoji 图标逻辑（属"真实素材"范畴，留待用户提供正式图）；og.png 已被 index/download/product 三页 OG/Twitter 卡片引用，icon-512 经 manifest 被 PWA 安装与社交抓取使用。
- 验证：manifest JSON 合法（icons 含两项）；/manifest.json、/assets/img/og.png、/assets/img/icon-512.png 均 200 且 content-type 正确；og.png 在首页源码 og:image 引用确认。
- 注：生成图默认落在项目根 generated-images/，已 mv 至 assets/img/ 并清理空目录。

## 批次3 收尾 + 批次4（明暗主题 / 中英双语 / Docker-CI / API 文档）
- 批量任务44 完成：主题切换、i18n 中英双语、Docker 化与 CI、API 文档页。
- 明暗主题：`:root` CSS 变量 + `[data-theme="light"]` 覆盖；head 内联脚本提前应用防闪烁；`assets/js/theme.js` 处理持久化与 theme-color meta；8 个页面（index/download/contact/product/admin/api/404/offline）统一加切换按钮。
- 中英双语 i18n：`assets/js/i18n.js` 轻量引擎（dict + data-i18n / data-i18n-ph / data-i18n-title，localStorage 持久化，缺失 key 回退原文绝不空白）；导航/页脚/按钮/后台面板 chrome 全部可切换；后台动态看板（KPI/产品表/留言表/状态标签）经 T() 助手 + `gr:langchange` 事件重渲染，contact.js 提交中态也走词典。
- 关键修复：i18n.js 第 41 行缺尾逗号导致整文件解析失败（语言切换静默失效），已补逗号并通过 `node --check`；另增 action.shelfOn/Off、btn.sending 等缺失 key，en 块补齐 product.edit/delete/status.mark*。
- API 文档页 `api.html`：23 个端点徽章、按方法着色 + 鉴权等级；从各页脚与后台头部进入。
- Docker：`Dockerfile`（多阶段编译 better-sqlite3，/app/data 挂载卷）、`.dockerignore`；`.github/workflows/ci.yml`（冒烟自检 `--spawn` + 镜像构建校验）。
- 验证：smoke-test 全 96 项通过；8 页 lang-toggle + data-i18n 计数正常；api.html 补齐后 toggle=1/data-i18n=9；T() 用到的 20 个 key 全部存在于词典。
- 外部资源（真实安装包/品牌素材/SMTP/服务器+域名）仍属待用户提供项，未实现（框架已留）。
