# 2026-09-02 工作日志

## 多语言切换：中英二元 → 6 语言下拉选择器（维度 42 延伸）
- 用户要求「增加多语言切换」，经确认扩展到 **ja/ko/es/fr**（保留 zh/en），切换控件用**下拉菜单**。
- `assets/js/i18n.js` 重写：
  - DICT 新增 ja/ko/es/fr 完整词典（每语言 125 个 key，无缺失/空值，已脚本校验）。
  - 新增 `LANGS=['zh','en','ja','ko','es','fr']` 与 `LANG_NAMES`（各语言自名称 endonym，不进 DICT）。
  - 二元 `toggle()` 改造为**下拉选择器**：`buildSelect()` 把 `.lang-select` 空容器填充为 `按钮(aria-haspopup=listbox) + ul.lang-menu(role=listbox, 6 个 li[role=option][data-lang])`；点击展开/收起、选项设置语言、外部点击/Esc 关闭、当前项 `.active` 高亮 + `aria-selected`、`gr:langchange` 仍派发（后台动态看板重渲染）。
  - 原 `lang.toggle`/`lang.current` key 已弃用（下拉用 LANG_NAMES 固定显示）。
- `assets/css/style.css`：`.lang-select/.lang-toggle/.lang-current/.lang-caret/.lang-menu/li` 样式，暗色 + `[data-theme="light"]` 下可读，菜单 `z-index:200` 定位按钮下方，当前项紫色高亮。
- 8 个页面（index/download/contact/product/admin/api/404/offline）旧 `<button class="lang-toggle">EN/中文</button>` 全部替换为 `<div class="lang-select"></div>`，由 JS 动态填充。
- 验证：`node --check` 通过；6 语言词典完整性脚本通过（125 key×6）；DOM stub 执行确认下拉运行时生成 6 选项且标签齐全；冒烟自检 **96 项全绿**；8 页 `lang-select=1 / i18n=1 / old-btn=0`。

## 待办（外部依赖，未动）
- 真实素材/SMTP/服务器域名仍待用户（上线前置）。

## 维度 42 收尾 + 维度 43：a11y 断言固化 / 登录契约修复 / 结构化数据收尾

### 1. 维度 42 a11y 断言固化（补回被中断项）
- `tools/smoke-test.js` 新增 `[10] 无障碍 (a11y) 护栏`（读取仓库文件，不依赖运行实例）：
  - 8 页均含 `skip-link` + 主内容地标（`id="main"`/`role="main"`）；
  - `admin.html` 全部 `<label for>` 命中对应控件 id（无孤儿 label）；产品表单 6 字段齐全；
  - `main.js` 汉堡切换同步 `aria-expanded`；
  - 4 个营销页（index/download/contact/product）恰有 1 个 `aria-current="page"`。

### 2. 修复冒烟自检登录契约 bug（重要）
- 根因：dim-31 多管理员后登录接口为「用户名+口令」，但冒烟用例长期只发 `{password}`，缺 `username` → `verifyPassword('',pw)` 必 401。
- 影响：`--spawn`（CI 使用）登录断言长期假性失败。修复：登录/错误口令用例补 `username:'admin'`，恢复真实契约校验。

### 3. 维度 43：结构化数据 (SEO 富媒体) 收尾
- `download.html`/`contact.html` 加静态 `BreadcrumbList` JSON-LD（占位域名由服务端中间件替换为真实 SITE，与首页 Organization 同机制）。
- `assets/js/product.js` 的 `renderDetail` 改为注入 `@graph`：`SoftwareApplication`（补全 `image`/`brand`）+ 三级 `BreadcrumbList`（首页>下载中心>产品名）。
- `tools/smoke-test.js` 新增 `[11] 结构化数据`：校验 download/contact 合法 JSON-LD + BreadcrumbList、product.js 注入能力、首页 Organization 回归。

### 验证
- `--spawn` 全量 **125 项全绿**（新增 a11y 20 + 结构化 9，并修复登录契约 2 项假性失败）。
- 运行实例 3000 实时生效：download/contact 含 BreadcrumbList；product.js `node --check` 通过。

## 维度 44：后台危险操作「二次确认弹窗」强化（替换原生 confirm()）
- 新建 `assets/js/confirm-modal.js`（UMD：`window.showConfirm` + node `module.exports`，供测试）：`showConfirm(opts)` 返回 `Promise<boolean>`；`role=dialog`/`aria-modal`/自动聚焦确认键/`Esc` 取消/点遮罩取消/`Enter` 确认；危险态加 `modal-danger` + `btn-danger` 红键。
- `admin.html` 在 `admin.js` 前引入 `confirm-modal.js`（243 行）。
- `assets/js/admin.js`：产品 del 按钮加 `data-name`（回显产品名）；留言删除、产品删除两处 `confirm()` 改为 `showConfirm({title,message,danger,confirmText})`，fetch 链移入 `.then` 内仅 ok 时执行；不再有任何原生 `confirm(`。
- `assets/js/i18n.js`：6 语言补全 4 个 key（`contact.deleteTitle`/`contact.deleteConfirm`/`product.deleteTitle`/`product.deleteConfirm`，各语言 1 处共 6 处），避免非中文显示原始 key。
- `assets/css/style.css`：新增 `.modal-overlay/.modal/.modal-title/.modal-desc/.modal-actions/.btn-danger` 等弹窗样式（暗色+`[data-theme=light]` 兜底变量、动画、`prefers-reduced-motion` 关闭）。
- 新建 `tools/_verify_confirm.js`（DOM 桩）：模拟 点击确认/取消键、Enter、Esc、点遮罩，校验 A11y 结构/危险态样式/聚焦/cleanup，**16 项全过**。
- `tools/smoke-test.js` 新增 `[12] 后台危险操作二次确认弹窗`：admin.html 引脚本、admin.js 改用 showConfirm 且无原生 confirm、6 语言 key 齐全、运行 `_verify_confirm.js` 退出 0。
- README 回归自检表更新：134 项 / 12 组（新增 a11y、结构化、二次确认三行）。

### 验证
- `node tools/smoke-test.js --spawn` → **134 项全绿**（本轮 +9：i18n 4 key×6 校验、弹窗 3 文件断言、行为校验 1）。
- 运行实例 3000：confirm-modal.js 返回 200，admin.html 含脚本引用，style.css 含 `.modal-overlay`。
