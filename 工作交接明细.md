# 绿角犀官网 · 工作交接明细

> 生成时间：2026-09-03　|　最近更新：2026-09-12　|　当前状态：**已上线生产（阿里云 47.116.59.141）**；本地回归 **749 项全绿 / 45 组**，生产巡检 **746 项全绿**
> 适用对象：接手维护 / 继续开发本项目的同事
> 维护方式：本文件与 `README.md`、`DEPLOY.md`、`.workbuddy/memory/` 日志配合使用

---

## 0. 一句话定位

公司官网 + 产品下载中心 + 后台数据管理。**零构建静态前端 + Node.js/Express/SQLite**，可直接静态托管，也提供 Docker / CI 流水线。

---

## 1. 现状快照（交接必读）

| 项 | 状态 |
| --- | --- |
| 本地预览 | `http://localhost:3000`（后台 `admin.html`，口令 `admin123`） |
| 回归自检 | 本地 `node tools/smoke-test.js --spawn` → **749 项全绿 / 45 组**；生产巡检 → **746 项全绿**（2026-09-12 实测） |
| 功能维度 | 维度 1–80 已交付（详见第 4 节） |
| 数据库 | 本地 `data/app.db`；生产 `/opt/lvjiaoxi-web/data/app.db`（SQLite WAL，自动备份至 `data/backups/`，约每日 1 份保留 7 份） |
| 生产部署 | **已上线**：阿里云 `47.116.59.141`，PM2 进程 `lvjiaoxi-web`（端口 3002），nginx 反代 `lvjiaoxi.com`（速查见第 12 节） |
| 上线前置 | 剩余：DNS A 记录、SSL 证书、真实安装包/素材、SMTP（详见第 8 / 11 节，未提供，切勿 mock） |

---

## 2. 技术栈

| 层 | 选型 |
| --- | --- |
| 前端 | 原生 HTML / CSS / JS（IntersectionObserver 触达动画，**零构建**） |
| 后端 | Node.js + Express + better-sqlite3（文件数据库，免安装） |
| 鉴权 | 多管理员（scrypt + 角色 admin/viewer）+ Bearer Token（前端 localStorage，8h 有效期） |
| i18n | 自研轻量引擎 `assets/js/i18n.js`（**6 语言** zh/en/ja/ko/es/fr 下拉切换） |
| 部署 | 零构建静态 + Node 服务；`Dockerfile` 多阶段构建 + GitHub Actions CI（冒烟 + 镜像校验） |
| PWA | `manifest.json` + `sw.js` 离线缓存 |

---

## 3. 目录结构与关键文件

```
.
├── index.html / download.html / contact.html / product.html   # 营销页（运行时注入真实域名）
├── admin.html              # 后台（多管理员 + Bearer Token）
├── api.html                # API 文档页
├── 404.html / offline.html # 错误/离线页
├── assets/
│   ├── css/style.css       # 全局样式 + 触达动画 + 明暗主题变量 + 弹窗样式
│   ├── js/
│   │   ├── main.js         # 触达动画 + 埋点 + 首页产品拉取 + 汉堡菜单(aria-expanded)
│   │   ├── admin.js        # 后台全部逻辑（登录/看板/产品/留言/审计/备份）
│   │   ├── product.js      # 产品详情页 + JSON-LD 注入
│   │   ├── i18n.js         # 6 语言词典 + 下拉选择器 + gr:langchange
│   │   ├── confirm-modal.js# UMD 二次确认弹窗（showConfirm）
│   │   └── theme.js        # 明暗主题切换
│   └── img/                # 品牌图（og.jpg 等，待替换为正式素材）
├── server/
│   ├── server.js           # Express 入口：静态托管白名单 + 中间件 + 域名替换 + 缓存策略 + 健康检查
│   ├── db.js               # SQLite 建表 + 种子数据（4 应用 + 4 游戏）
│   ├── auth.js             # 口令(scrypt) + Token 签发/校验
│   └── routes.js           # 全部 API 路由（含产品双重校验、留言限流、审计）
├── tools/
│   ├── smoke-test.js       # 零依赖回归自检（本地 749 项/45 组，生产 746 项，核心护栏）
│   ├── _verify_confirm.js  # 弹窗行为桩测试（DOM mock）
│   └── maintain.js         # 备份/校验/清理/恢复 运维工具
├── data/app.db             # 自动生成；删 *.db* 重启重灌种子
├── downloads/              # 安装包投放目录（README.md 说明）
├── DEPLOY.md               # PM2/systemd/nginx/HTTPS/备份/升级 完整手册
└── Dockerfile / .github/workflows/ci.yml
```

---

## 4. 功能维度演进（1–80）

> 维度追踪以根目录 **`维度清单.md`** 为唯一索引（80 维总表 + 回归组映射）；本文第 4 节为展开。维度 1–41 为早期基础框架，下面仅列要点；42–80 为近期高密度交付，单独展开。

### 4.1 维度 1–41（基础框架，要点）
- 官网/下载/后台/联系/产品/API 六类页面 + 404/offline + PWA。
- 后端：Express 入口、SQLite 建表与种子、多管理员鉴权（scrypt + 角色）、Bearer Token。
- 埋点（浏览/下载上报）、明暗主题（无闪烁）、触达动画。
- 安全基线：静态托管白名单（13 敏感路径 404）、CSP/HSTS/防嵌套、不泄露 `X-Powered-By`、请求 ID 追踪、`X-Request-Id` 格式白名单。
- SEO 基础：sitemap / robots / RSS（真实产品条目）/ canonical / OG 图。
- 留言防滥用：蜜罐 + 邮箱校验 + 按 IP 限流。
- 可观测性：公开 `/api/health` + 需登录 `/api/health/detail`（503 告警）、统一错误处理（不泄露堆栈）、日志轮转。
- 运维：自动备份 + 即时完整性自证（`integrity_check`）、`tools/maintain.js` 全套工具、审计日志覆盖关键动作。
- 容器化：多阶段 `Dockerfile` + CI 跑冒烟并校验镜像可构建。
- i18n 初版：中英双语（后被维度 42 扩展为 6 语言）。

### 4.2 维度 42 — 多语言 6 语言下拉选择器
- `assets/js/i18n.js` 重写：每语言 **125 key** 完整词典（无缺失/空值，已脚本校验）；新增 `LANGS=[zh,en,ja,ko,es,fr]` 与 `LANG_NAMES`（endonym 显示名）。
- 二元 `toggle()` → **下拉选择器** `buildSelect()`：按钮(`aria-haspopup=listbox`) + `ul.lang-menu`(`role=listbox`，6×`li[role=option]`)；点击展开/收起、外部点击/Esc 关闭、当前项 `aria-selected`、派发 `gr:langchange`（后台动态重渲染）。
- 8 个页面旧 `<button class="lang-toggle">` 全部替换为 `<div class="lang-select">`（JS 注入）。
- 回归：词典完整性脚本 + DOM stub 确认运行时 6 选项。**（该维度曾因网络中断打断，断言已在维度 43 阶段固化。）**

### 4.3 维度 43 — a11y 断言固化 / 登录契约修复 / 结构化数据收尾
- **a11y 护栏 `[10]`**：8 页含 `skip-link` + 主内容地标；`admin.html` 全部 `<label for>` 命中控件（无孤儿 label）；汉堡切换同步 `aria-expanded`；营销页恰 1 个 `aria-current="page"`。
- **修复冒烟登录契约 bug（重要）**：多管理员后登录需 `{username, password}`，但冒烟用例长期只发 `{password}` → `verifyPassword('',pw)` 必 401，**导致 `--spawn`（CI）登录断言长期假性失败**。已补 `username:'admin'`。
- **结构化数据 `[11]`**：`download.html`/`contact.html` 加静态 `BreadcrumbList` JSON-LD（占位域名由服务端中间件替换）；`product.js` 注入 `@graph`：`SoftwareApplication`(补 `image`/`brand`) + 三级 `BreadcrumbList`；首页 `Organization` 回归。

### 4.4 维度 44 — 后台危险操作二次确认弹窗
- 新建 `assets/js/confirm-modal.js`（UMD：`window.showConfirm` + node `module.exports`）：`showConfirm(opts)` 返回 `Promise<boolean>`；`role=dialog`/`aria-modal`/自动聚焦/`Esc` 取消/点遮罩取消/`Enter` 确认；危险态 `modal-danger` + `btn-danger` 红键。
- `admin.js`：留言删除、产品删除改用 `showConfirm()`（产品删除回显名称），fetch 链仅确认时执行；**原生 `confirm()` 已彻底移除**。
- `i18n.js` 6 语言补 4 key（删除标题/确认文案）；`style.css` 补弹窗样式（含 `prefers-reduced-motion` 降级）。
- `tools/_verify_confirm.js`（DOM 桩）：16 项行为校验全过。**`[12]` 组** 固化。

### 4.5 维度 45 — 产品表单双重校验
- **服务端 `routes.js`**：新增 `/admin/products` POST 与 PUT 的 `validateProductInput`——trim、名称长度上限、下载链接协议白名单（**拒 `javascript:`/`data:`/`vbscript:`**，允许 http(s) 与站内相对路径）。
- **客户端 `admin.js`**：表单提交先做同源校验；**修复「校验失败也假成功」缺陷**（此前不检查 `r.ok`，400 仍 `resetForm()+reload`）→ 现检查 `r.ok`，失败 `showToast('保存失败…')`。
- **`admin.html`**：表单字段加 `maxlength` 第一道防线。
- **`[13]` 组**：服务端 400 用例（空名/超长/危险协议）+ 客户端无原生 `confirm` 残留 + 运行实例真实登录后 POST 校验。

### 4.6 维度 46 — 错误页增强 / 表单校验 / 图片优化 / SEO（2026-09-09）
- **404 / offline 增强**：404 加快捷导航 + 站内搜索（`assets/js/err-search.js`，外置脚本符合 CSP，拉 `/api/products` 预建索引）；offline 加快捷导航，内联状态脚本移到 `assets/js/offline.js`（修复 CSP 违规）。两页新脚本均入 `sw.js` 预缓存。
- **后台表单校验强化**：服务端 `routes.js` 对分类做白名单字符集校验（拒 HTML/控制字符）、图标限 emoji；客户端 `admin.js` 同源校验 + `admin.html` 分类联想 `datalist`。
- **图片优化**：全部 `<img>` 补 `decoding="async"` + 显式 `width/height`（防 CLS），首屏 logo 保持不懒加载（保 LCP）。
- **SEO**：`sitemap.xml` 动态 `<lastmod>`（取静态页 mtime）、RSS 加 `<category>` 与 `<language>` 标记。

### 4.7 维度 47 — prefers-reduced-motion 无障碍动效降级（2026-09-09）
- 新建 `assets/js/motion.js`：`window.prefersReducedMotion()`，matchMedia 一次性缓存 + `change` 时刷新。
- `main.js`：数字滚动（count-up）在减弱动态下直接落终值、分类跳转平滑滚动改即时；`admin.js`：编辑表单滚动同理。
- `index/download/contact/admin` 四页引入 `motion.js`；`sw.js` 预缓存。CSS 侧此前已有全局 `prefers-reduced-motion: reduce` 降级（transition/animation 归零），本维度补齐 JS 侧动态。

### 4.8 维度 48 — 404 搜索键盘导航与结果高亮（2026-09-09）
- 404 搜索改为标准 combobox + listbox 交互：输入框 `role="combobox"` + `aria-autocomplete="list"` / `aria-expanded` / `aria-activedescendant`；结果 `role="option"`。
- `err-search.js`：↑/↓ 键盘导航（循环）、Enter 选中当前（无高亮则取第一条）、Esc 关闭；鼠标悬停同步高亮；`announce()` 播报「找到 N 个结果 / 未找到」至 `aria-live="polite"` 隐藏区（`#err-live`）。
- `style.css`：新增 `.err-result.active` 渐变高亮、`.err-results:focus-visible`、全局 `.visually-hidden` 工具类。
- 无 JS 时原生 `<input type="search">` 仍可用（渐进增强）。

### 4.9 维度 49 — 后台留言批量操作（2026-09-09）
- 后端新增两个批量端点（均 admin 角色、单次上限 100、写审计日志）：`POST /api/admin/contacts/batch-status`（批量改处理状态）、`POST /api/admin/contacts/batch-delete`（批量删除，审计含邮箱便于追溯）。
- 前端 `admin.js`：留言条目加勾选框 + 工具栏「全选本页」（跨页保留选择集）；批量操作条显示「已选 N 条」，支持「标记已处理 / 删除选中」（删除走 `showConfirm` 二次确认）；数据刷新/筛选切换时清理幽灵选中。
- `admin.html`：全选控件 + 批量条（`data-admin-only`，viewer 角色自动隐藏）；`style.css` 补 `.msg-check` / `.contact-batch` 样式。
- i18n 6 语言新增 6 key（全选本页 / 标记已处理 / 删除选中 / 已选 N 条 / 批量删除标题与确认）；`api.html` 文档同步两个批量端点。

### 4.10 维度 50 — 维度清单总表（2026-09-09）
- 新建根目录 `维度清单.md`：49 维一行式总表 + 统计 + 章节映射 + 回归组↔维度对照，收敛「维度追踪分散在日志/README/本文档」的缺口。
- 交接文档第 4 节改为引用该文件为唯一索引；第 9 节「无单一索引」风险项移除。
- 冒烟新增 `[18]` 组 4 条断言：总表存在、含 49 个维度行、统计数字、回归组映射。

### 4.11 维度 51 — 后台留言服务端分页/搜索/筛选（2026-09-10）
- **后端 `routes.js`**：`GET /api/admin/contacts` 改为服务端分页——`page`（默认 1）、`pageSize`（默认 10，上限 100 钳制）、`keyword`（姓名/邮箱/留言三列 LIKE 模糊）、`status`（new/done 过滤），返回 `{rows, total}`；`/api/contact` 限流从 5 次放宽至 30 次/10 分钟（配合测试数据生成，仍防滥用）。
- **前端 `admin.js`**：移除客户端 `filteredContacts` 全量过滤，改 `loadContacts()` 按分页参数请求；搜索 300ms 防抖；删除末页数据后自动回退一页；`selectedIds` 跨页保留（仅清理本页消失项）。
- **冒烟 `[19]` 组**：分页两页数据、`total` 计数、keyword 精确命中、status 筛选、pageSize 上限钳制、测试数据清理、admin.js 静态断言（防抖/keepPage/服务端 total/无 filteredContacts）。
- **文档**：`api.html` 分页参数与限流说明同步；`api.html` 对应条目更新。

### 4.12 维度 52 — 404 搜索模糊匹配（分类/描述）（2026-09-10）
- **`err-search.js`**：搜索从「仅名称包含」扩展为「名称 / 分类 / 类型 / 描述」任一包含即命中；`matchRank` 相关度排序（名称命中 0 > 分类/类型 1 > 描述 2，稳定排序保原序）；结果仍限 6 条。
- **命中提示**：非名称命中给一行上下文说明——`分类 · X` / `类型 · Y` / `描述 · …片段…`（描述截取命中词前后共 ~20 字）；新增 `.err-result-main`（两行布局）与 `.err-result-hint` 样式，active 态同步反白。
- **i18n**：新增 `err.matchCat` / `err.matchType` / `err.matchDesc` 3 key × 6 语言（分类/类型/描述）。
- **冒烟 `[20]` 组**：err-search.js 静态断言（四字段模糊、matchRank 排序、slice 6、hint、desc 截取）+ style.css `.err-result-hint` + i18n 6 语言完整性 + `/api/products` 运行时含 category/desc 字段。

### 4.13 维度 53 — 管理员角色编辑与账号删除（2026-09-10）
- **后端 `routes.js`**：新增 `PUT /api/admin/admins/:id`（改角色，body `{role}` 白名单校验，角色未变幂等返回）与 `DELETE /api/admin/admins/:id`（删账号）；`auth.js` 新增 `updateAdminRole` / `deleteAdmin`。均写审计日志（`admin_role` / `admin_delete`，含 id+username）。
- **防锁死保护（关键）**：最后一个 admin 角色账号不可降级为 viewer、不可删除（否则后台将无人可写操作）；不可删除当前登录账号。前端对自身行直接禁用控件，后端兜底校验。
- **前端 `admin.js`**：管理员列表每行改为「用户名 + 当前账号标记（`.admin-self`）+ 角色下拉 `<select class="admin-role-select">` + 删除按钮」；角色切换与删除均走 `showConfirm` 二次确认（确认弹窗回显用户名，删除为危险红键）；语言切换时重渲染列表。
- **i18n**：6 语言新增 7 key（admin.self / admin.delete / admin.roleTitle / admin.roleConfirm / admin.confirm / admin.deleteTitle / admin.deleteConfirm）。
- **`style.css`**：新增 `.admin-self`、`.admin-role-select`（含 focus-visible、disabled、sel-admin/sel-viewer 着色）样式，移除不再使用的 `.admin-role` 静态徽章样式。
- **`api.html`**：文档同步 PUT / DELETE `/api/admin/admins/:id`（含防锁死说明）。
- **冒烟 `[21]` 组**：运行时（防锁死降级 400、新增/降级/提级/删除闭环、非法角色 400、幂等 200、删自己 400、删不存在 404、非法 id 400、测试账号全量清理）+ 静态（admin.js 下拉/删除/自身禁用/showConfirm、routes.js 两端点与防锁死文案、i18n 4 key×6 语言、api.html 文档）。

### 4.14 维度 54 — PWA 新版本更新提示（2026-09-10）
- **背景缺口**：pwa.js 原先仅注册 Service Worker；发布新版本后，老用户停留在旧页面时**毫无感知**（静态资源走 stale-while-revalidate，刷新才换新版）。
- **`assets/js/pwa.js` 重写**：注册后监听 `updatefound` → 新 SW `installed` 且页面已被旧 SW 控制（非首次安装）时，弹出底部提示条「发现新版本，点击刷新」；点击「刷新」→ `postMessage({type:'SKIP_WAITING'})` 给 waiting worker + `location.reload()`。暴露全局 `window.checkPWAUpdate()`（`registration.update()` 手动探测）；每小时 `setInterval` 自动探测；监听 `gr:langchange` 重翻译提示条。首次安装静默，不打扰访客。
- **`sw.js`**：新增 `message` 监听处理 `SKIP_WAITING`（配合提示条点击立即接管）；原有 install skipWaiting / activate 清理逻辑不变。
- **`style.css`**：新增 `.pwa-update-bar`（底部居中、accent 描边、primary-grad 按钮、关闭钮、`pwa-bar-in` 入场动画），明暗主题随 CSS 变量自适应。
- **后台入口**：`admin.html` 补引 pwa.js + 系统信息面板头部「↻ 检查更新」按钮（`#check-update`），admin.js 绑定点击 → `window.checkPWAUpdate()` + toast 反馈（3 秒防重复）。
- **i18n**：6 语言新增 4 key（pwa.update / pwa.refresh / pwa.close / btn.checkUpdate）。
- **冒烟 `[22]` 组**：17 项静态断言（pwa.js updatefound/controller 判定/checkPWAUpdate/SKIP_WAITING/langchange/定时探测、sw.js message 监听、style.css 提示条样式、i18n 4 key×6 语言、4 页引用 pwa.js、admin.html 按钮 + admin.js 绑定）。**浏览器实测通过**（2026-09-10）：本地服务器修改 sw.js 字节模拟发版 → `checkPWAUpdate()` → 提示条弹出 → 点击刷新 → 页面 reload 且提示条消失。

### 4.15 维度 55 — 产品置顶/排序权重（2026-09-10）
- **数据库 `server/db.js`**：幂等迁移新增 `products.sort`（INTEGER NOT NULL DEFAULT 0）；旧数据全部按 0 处理，排序回退到 ID 升序，零迁移风险。
- **后端 `server/routes.js`**：公开 `/api/products`（全部查询分支）与后台 `/admin/products` 均改按 `sort DESC, id ASC` 排序（权重越大越靠前 = 置顶）；`normalizeProduct` 增加 sort 解析与双重校验（**整数** + 范围 -9999~9999，空值视为 0），POST/PUT 均写入 sort。
- **前端 `admin.html` + `admin.js`**：产品表单新增「排序权重」数字输入（`type=number` step=1 min/max，placeholder 提示「越大越靠前（默认 0）」）；产品表格新增「排序」列（`.sort-cell`）；编辑回填、新增/编辑提交携带 sort；客户端同源校验；CSV 导出增加「排序」列。
- **i18n**：6 语言新增 3 key（`f.sort` 排序权重 / `f.sortPh` 占位提示 / `col.sort` 排序列头）。
- **`api.html`**：文档同步 `/api/products` 排序规则与 POST 的 sort 字段说明。
- **冒烟 `[23]` 组**：静态（db.js 迁移、routes.js 两端排序与校验与写入、admin.js 表单/回填/排序列、admin.html 输入与表头、i18n 3 key×6 语言）+ 运行时（建 3 个不同权重产品 → 公开接口严格按 500>100>-10 返回；后台接口带 sort；非法/越界权重 400；编辑权重后置顶即时生效；测试数据全量清理）。

### 4.16 维度 56 — 流量趋势（可切换天数）（2026-09-10）
- **背景**：看板趋势图此前仅有固定 14 天（stats 内嵌 `daily`），无法观察更长/更短周期，且 `bucketDaily` 为全表逐条遍历 O(N·days)，数据量大时浪费。
- **后端 `server/routes.js`**：新增独立端点 `GET /api/admin/stats/trend?days=N`（需 Token；days 钳制 1~90，非法/缺省回退 14），返回 `{days, total:{visits,downloads}, range:[{date, visits, downloads}]}`；`bucketDaily` 重写为 **SQL 聚合**（`strftime('%Y-%m-%d', ts/1000,'unixepoch','localtime')` 分组 + JS 本地时区补零，缺失日期补 0）；原 `/admin/stats` 仍返回 14 天 `daily` 字段（向后兼容）。
- **前端 `admin.html` + `admin.js`**：趋势区块独立渲染——`loadTrend()`/`applyTrend()`；标题动态显示「流量趋势 · 近 N 天」；新增 7/14/30 天切换按钮组 `#trend-days`（`.td-btn`，active 态 `aria-pressed`）；图下新增区间合计行 `#trend-total`（浏览/下载合计）；窗口缩放重绘改用 `lastTrend`；`gr:langchange` 时重渲染按钮/标题/合计；旧 `lastDaily` 彻底移除。
- **i18n**：`panel.trend` 去固定天数文案（6 语言通用化）；新增 `trend.days`（近 {n} 天）/ `trend.dayUnit`（天）/ `trend.total`（区间合计）3 key × 6 语言。
- **`style.css`**：新增 `.trend-days` / `.td-btn`（hover/active/focus-visible）/ `.trend-total` 样式。
- **`api.html`**：文档同步 `/api/admin/stats/trend?days=`。
- **冒烟 `[24]` 组**：静态（routes.js 端点/钳制/total/SQL 聚合、admin.js loadTrend/预设/切换/标题合计/语言重渲染/无 lastDaily、admin.html 按钮与合计行、style.css 样式、i18n 3 key×6 语言与 panel.trend 通用化）+ 运行时（days=7/90 长度正确、9999/abc 钳制 14、每项结构合法且 total=区间求和、未带 token 401、stats 兼容仍含 14 天 daily）。

### 4.18 维度 58 — 产品分类后台维护（2026-09-10）
- **背景**：产品分类此前为后台表单自由输入（前端 datalist 联想），无统一枚举；分类一旦写错/口径不一，下载页筛选与展示随之混乱，也无法后台统一维护。
- **后端 `server/db.js`**：新增 `categories` 表（`name TEXT NOT NULL UNIQUE` + `created_at`）；启动迁移 `migrateCategories`（幂等）：表为空时从现有产品分类 `SELECT DISTINCT category` 回填，并补齐常用分类（效率/协作/工具/安全/RPG/休闲/竞速/复古）。
- **后端 `server/routes.js`**：
  - 公开 `GET /api/categories` → `[{id, name}]`（下载页分类区自动生成的数据源）。
  - 后台 `GET /api/admin/categories` → `{rows:[{id,name,created_at,usage}]}`（usage=引用产品数，供删除保护与运营参考）。
  - `POST /api/admin/categories` 新增（`normalizeCategoryName` 校验：非空/≤40 字符/白名单字符集；重名 409；审计 `category_add`）。
  - `PUT /api/admin/categories/:id` 重命名（事务内同步 `UPDATE products SET category=? WHERE category=?` 防孤儿；重名 409；审计 `category_rename`）。
  - `DELETE /api/admin/categories/:id` 删除（`usage>0` 返回 409「无法删除」；审计 `category_delete`）。
  - 写操作全部 `requireAdmin`；产品新增/编辑校验：`category` 非空时必须为枚举项（否则 400），分类字符集正则补充允许下划线。
- **前端 `admin.html` + `admin.js`**：新增「产品分类管理」面板（新增表单 + 列表行：名称/使用产品数/重命名(行内输入)/删除(二次确认) + 刷新按钮）；产品表单分类由 datalist 联想改为**枚举下拉**（`#f-category`，空=不分类，选项来自 `/api/admin/categories`，保持当前选中值）；分类变更后自动刷新产品列表。
- **下载页 `main.js`**：分类下拉 `#category-filter` 优先从公开 `/api/categories` 后台枚举构建，失败时兜底从已加载产品收集分类名。
- **`style.css`**：新增 `.cat-list` / `.cat-row` / `.cat-rename-input` 等分类管理样式。
- **i18n**：新增 `panel.categories` / `cat.none` / `cat.name` / `cat.add` / `cat.usage` / `cat.rename` / `cat.delete` / `cat.delTitle` / `cat.confirmDel` / `cat.save` / `cat.empty` / `cat.needName` 12 key × 6 语言。
- **`api.html`**：文档同步 `/api/categories` 与后台分类 CRUD 端点。
- **冒烟 `[26]` 组**：静态（db.js 建表/回填迁移、routes.js 公开+CRUD+requireAdmin 审计/校验/重命名同步/使用中禁删/枚举校验、admin.html 面板与枚举下拉、admin.js 管理函数与变更联动、style.css 样式、main.js 下载页枚举下拉含兜底、i18n 12 key×6 语言）+ 运行时（公开枚举含种子分类、新增→id、重名 409、空名 400、未知分类产品 400、枚举分类可建产品、使用中删 409、重命名 200 且产品同步新名、重名改 409、删产品后删分类 200、viewer 写 403、增删后公开接口仍可用）。

### 4.19 维度 59 — 产品大图/缩略图（2026-09-10）
- **背景**：产品卡片此前仅 emoji 图标，无视觉大图；候选列表第 5 项。为下载页/首页卡片、详情页与后台弹窗补上产品大图，同时明确「空值回退 emoji」的容错路径。
- **后端 `server/db.js`**：`products` 表新增 `image` 字段（`TEXT NOT NULL DEFAULT ''`，幂等迁移）；迁移 `migrateProductsImage` 仅在 `image=''` 时按产品名回填 8 张种子默认图（兼容旧库，不覆盖已配置值）。
- **素材 `assets/img/products/`**：8 张产品大图（效率笔记/团队协作/云盘同步/密码管家/星际探险/消除乐园/赛车竞速/像素勇者），统一深蓝底 + 紫青渐变风格，已去 AI 水印并压缩至 18–49KB。
- **后端 `server/routes.js`**：
  - 公开/后台产品查询（列表/详情）全部返回 `image` 字段。
  - `validProductImage()` 白名单校验：空值合法（回退 emoji）、`http(s)://` 外链、或站内 `/assets/img/…` 图片路径（jpg/png/webp/avif/gif/svg），拒绝 `javascript:`/`data:` 等危险协议与任意外链；超 500 字符拒绝。
  - 新增/编辑产品（POST/PUT）写入 `image`，双重校验不通过返回 400。
- **后台 `admin.html` + `admin.js`**：产品表单新增「产品图」输入（`#f-image`，maxlength=500，placeholder 示例两种合法格式）；提交时携带 `image` 并做客户端同规则预校验；编辑时回填。
- **前端渲染**：
  - `main.js`：下载页卡片 `cardHTML` 与首页 `homeCardHTML` 有 `image` 时渲染 `<img class="pimg">`（`loading="lazy"` + `decoding="async"` + 固定 960×850 防 CLS），无图回退 `.picon` emoji 大图标；详情弹窗 `pd-img` 同步填充（弹窗内 `img` 补 width/height）。
  - `product.js`：详情页主图 `.pd-img`（无图回退 `.pd-icon`）；OG 分享图在有图时改用产品图，否则兜底站内 og.jpg。
  - `style.css`：`.pmedia`（卡片顶部通栏媒体区，负 margin 消除内边距）、`.pimg`（cover 铺满）、`.picon-mini`（信息行小图标）、`.pd-img`/`.pd-icon` 等样式。
- **`sw.js`**：`ASSETS` 预缓存 8 张产品图，离线时卡片/详情仍可见。
- **i18n**：新增 `f.image` / `f.imagePh` 2 key × 6 语言。
- **`api.html`**：文档同步 `/api/products` 与 `POST /api/admin/products` 的 `image` 字段说明。
- **冒烟 `[27]` 组**：静态（db.js 迁移/回填、routes.js 查询含 image/validProductImage/INSERT·UPDATE 写 image、admin.html `#f-image`、admin.js 读取/校验/回填、main.js/product.js 渲染、style.css 样式、sw.js 预缓存、8 张图片文件存在、i18n 2 key×6 语言）+ 运行时（`javascript:`/`data:` 400、站外 http 200、站内路径新增返回 id 且公开接口返回 image、空值 200、测试数据清理）。
- **附注**：本维度顺带修复 `download.html` 详情弹窗 `img` 缺 width/height 的 CLS 隐患（冒烟 [14] 组回归）。

### 4.20 维度 60 — 全站 img 响应式（srcset/sizes）（2026-09-10）
- **背景**：候选列表第 1 项。原判定「站点无栅格 `<img>` 内容图、候选不适用」——前提已随维度 59 的产品大图改变：8 张 960×850 产品图在卡片/弹窗/详情三处渲染，1x 屏加载 960w 原图浪费带宽，需按视口宽度选档。
- **素材 `assets/img/products/*-sm.jpg`**：用 Windows 自带 .NET System.Drawing（PowerShell 一次性脚本，未引入 sharp 等新依赖）生成 8 张 480×425 缩略图，JPEG q85，单张 10–25KB（比 960w 原图省约 50% 流量）。
- **`main.js`**：
  - 新增 `srcsetList(src)`：站内 `/assets/img/` 图生成 `…-sm.jpg 480w, … 960w` 双档；外链无法保证配套变体 → 返回空串退回单图 `src`。
  - 新增 `IMG_CARD_SIZES`（`(min-width: 1000px) 354px, (min-width: 700px) 46vw, calc(100vw - 48px)`，与 `.product-grid` minmax(260px,1fr) 实际列宽吻合）与 `IMG_MODAL_SIZES`（`(min-width: 520px) 384px, calc(92vw - 56px)`，对应弹窗 `min(440px,92vw)` 减内边距）。
  - 下载页 `cardHTML` 与首页 `homeCardHTML` 渲染 `srcset`/`sizes`；详情弹窗 `openModal` 填充 `pim.srcset`/`pim.sizes`（外链清空 srcset）。
- **`product.js`**：详情页 `pd-img` 加 `srcset`（`-sm.jpg 480w` + 原图 960w）与 `sizes="220px"`（对应 `.pd-img` 固定 220px 宽，max-width:100% 移动端由浏览器自行缩放）。
- **`sw.js`**：`ASSETS` 追加 8 张 `-sm.jpg` 缩略图预缓存，离线时双档仍可选。
- **i18n**：无新增 key（纯渲染属性，无文案）。
- **`api.html`**：无 API 变更，不改动。
- **冒烟 `[28]` 组**：静态（main.js `srcsetList`/`IMG_CARD_SIZES` 断点/cardHTML·homeCardHTML·openModal 的 srcset·sizes、product.js pd-img 220px、sw.js 预缓存、8 张 -sm.jpg 存在）+ 运行时（`GET note-sm.jpg` → 200 且 Content-Type 为图片、响应体为合法 JPEG（FFD8 头）、缩略图体积小于原图）。

### 4.17 维度 57 — 审计/访问日志服务端分页与筛选（2026-09-10）
- **背景**：审计日志/访问日志接口此前一次全量返回，日志增长后后台拉取与渲染卡顿；对标留言已实现的「分页/搜索/筛选」补齐。
- **后端 `server/routes.js`**：`GET /api/admin/audit` 改服务端分页——`page`（≥1）/`pageSize`（1~100 钳制，缺省 10）；`keyword` 对 `action/detail/ip` 三字段 `LIKE %kw%` 模糊；`action` 精确筛选；返回 `{rows, total}`（COUNT + LIMIT/OFFSET，按 ts DESC, id DESC 排序）。访问日志接口 `/admin/accesslog` 增加 `lines` 参数（100/300/500，非法回退 100）。
- **前端 `admin.html` + `admin.js`**：审计面板新增筛选工具条（搜索框 300ms 防抖、操作类型下拉）+ 分页条 `#audit-pager`（上一页/下一页 + 「第 x / y 页 · 共 n 条」）；搜索/筛选变化重置页码；末页数据被删空时自动回退一页；访问日志面板新增行数下拉（100/300/500）。
- **i18n**：新增 `search.audit`（搜索操作/详情/IP…）1 key × 6 语言；`admin.html` 筛选控件文案直接使用既有 `all.*` 分类文案复用。
- **`style.css`**：新增 `.audit-tools` / `.log-tools`（工具条布局）与 `.filter-select`（胶囊下拉）样式。
- **`api.html`**：文档同步 `/api/admin/audit?page=&pageSize=&keyword=&action=` 与 `/api/admin/accesslog?lines=`。
- **冒烟 `[25]` 组**：静态（routes.js page/pageSize 钳制/LIKE/action 筛选/{rows,total}、admin.js loadAudit 参数/末页回退/renderAuditPager/防抖/action 重置页码/accessLogLines、admin.html 四个控件、style.css 样式、i18n 6 语言）+ 运行时（分页每页 ≤10、action=login 精确、无命中 total=0、pageSize=999 钳 100、页码越界空列表 total 正确、未带 token 401、accesslog lines=500 ≤500、lines=abc 回退 100）。

### 4.21 维度 61 — 合规基础页（隐私政策 / 服务条款 / robots / sitemap）（2026-09-10）
- **背景**：用户确认的候选方向「合规基础页（推荐）」。站点此前无隐私政策/服务条款页，robots/sitemap 也未显式声明合规口径。
- **新增 `privacy.html` / `terms.html`**：完整法律文案页（信息收集范围、使用与保护、用户权利、Cookie 说明等），沿用站点暗色主题与导航/页脚结构；两页均带 skip-link + `<main id="main">` 地标（a11y 护栏回归通过）。
- **`server/server.js`**：robots.txt 增加 `Disallow: /downloads/`（安装包目录）并声明 `Sitemap:` 地址；sitemap.xml 增补 privacy/terms 两个 URL（含 lastmod），按 UTF-8 输出。
- **i18n**：页脚复用既有 `footer.privacy`/`footer.terms` 入口与 `legal.*` 文案键（6 语言）。
- **冒烟 `[29]` 组**：静态（privacy.html/terms.html 存在且含关键章节、robots.txt 含 Disallow /downloads/ 与 Sitemap、sitemap.xml 含 privacy/terms 条目）+ 运行时（两页 200）。

### 4.22 维度 62 — 产品更新日志（version / changelog）（2026-09-10）
- **背景**：用户确认的候选方向「产品更新日志」。下载/详情页此前只有简介，用户无法了解版本演进。
- **`server/db.js`**：products 表幂等新增 `version`（≤30）与 `changelog`（≤2000）字段，并按 8 个种子产品回填示例版本/日志（仅 version 为空时写入，后台可覆盖）。
- **`server/routes.js`**：`normalizeProduct` 解析/校验两字段长度；公开列表与详情查询透出 `version`/`changelog`；新增/编辑产品写入两字段。
- **后台 `admin.html` + `admin.js`**：产品表单新增「版本号」输入与「更新日志」多行文本（maxlength 30/2000 第一道防线 + 客户端/服务端双重校验）；表格新增版本列（`.ver-cell`）；编辑回填。
- **详情页 `product.js`**：标题旁渲染 `v版本` 徽标（`.pd-version`）；正文下方渲染更新日志卡片（`.pd-changelog`，white-space: pre-line 保留换行、超高滚动）；动态插入的 `.reveal` 由 `observeReveals()` 重新观察（修复详情异步加载后保持隐藏的可见性问题）。
- **i18n**：新增 `f.version/f.versionPh/f.changelog/f.changelogPh/col.version/pd.changelog` 6 key × 6 语言。
- **冒烟 `[30]` 组**：静态（db.js 迁移与回填、routes.js 查询/校验/写入、admin 表单/表格/回填、product.js 徽标与日志区与 observeReveals、style.css、i18n 6 key×6 语言）+ 运行时（新增带版本/日志产品 → 公开列表/详情透出、编辑后详情反映新版本、超长 400、种子回填校验、测试数据清理）。

### 4.23 维度 63 — 下载量展示+热门（downloads）（2026-09-10）
- **背景**：用户确认的候选方向「下载量展示+热门排行」。下载事件此前仅入表统计，公开接口不透出，访客无感知。
- **`server/db.js`**：downloads 表新增 `product_id` 索引（`idx_downloads_product`，幂等），按产品统计计数加速。
- **`server/routes.js`**：公开 `/api/products`（列表 4 种组合）与 `/api/products/:id` 均通过相关子查询透出 `downloads`（该产品累计下载次数）。
- **前端**：`main.js` 卡片右上角渲染下载量胶囊徽标（`.pcard-dl`，`fmtCount` 简写 1.2k/1.5M，title 精确值，首页/下载页卡片均生效）；`product.js` 详情页简介下方渲染 `↓ 1.2k 次下载`（`.pd-dl`）。
- **i18n**：新增 `pd.downloads` 1 key × 6 语言。
- **冒烟 `[31]` 组**：静态（db.js 索引、routes.js 列表/详情下载量子查询、main.js 徽标与 fmtCount、product.js、style.css、i18n 6 语言）+ 运行时（新增产品 → 上报 2 次下载 → 公开列表/详情 downloads≥2 → 删除产品并直连清理孤儿下载明细，避免污染总下载统计）。

### 4.24 维度 64 — 导航栏全站搜索（nav-search / ?q=）（2026-09-10）
- **背景**：用户确认的候选方向「导航栏全站搜索」。此前仅下载页内有搜索框，访客在其他页面无法直达产品检索。
- **9 个页面（index/download/product/contact/privacy/terms/api/404/offline）**：导航新增胶囊搜索框（`.nav-search`，原生 form GET 提交到 `download.html?q=…`，输入 `#nav-q`，圆形 🔍 按钮）；≤900px 隐藏（移动端用下载页自身筛选）。
- **`main.js`**：下载页读取 `?q=` 参数 → 套用 `state.kw` 过滤、回填筛选输入框、滚动到筛选区（减弱动效下即时跳转）；任意页面预填 `#nav-q` 保持状态。
- **i18n**：新增 `nav.searchPh/searchInput/searchBtn/searchForm` 4 key × 6 语言。
- **`style.css`**：`.nav-search` 胶囊样式（focus-within 高亮）。
- **冒烟 `[32]` 组**：静态（9 页均含 nav-search 表单与 #nav-q、main.js 读取 ?q=/联动/预填/滚动、style.css、i18n 4 key×6 语言）+ 运行时（`download.html?q=效率` → 200）。

### 4.25 维度 65 — 热门下载排行（2026-09-10）
- **背景**：用户确认的候选方向「热门下载排行专区（推荐）」。下载量数据已入表并在卡片展示，但缺少「按下载量排序」的聚合出口，用户无法一眼看到大家都在下什么。
- **`server/routes.js`**：新增公开 `GET /api/products/hot?limit=`（1~10 钳制、非法回退默认 5，仅 `status='on'`，按下载量降序、sort DESC、id 次级排序）；条目完整透出产品字段 + `downloads` 计数（相关子查询）。
- **前端 `download.html` / `index.html` + `main.js`**：热门下载区 `<section id="hot">`（含标题与副题），`loadHot()` 拉取渲染 `.hot-strip` 列表——TOP1 大卡（`.hot-item.top1`，名次徽标 `.hot-rank`，下载量徽标），其余普通卡；空态显示 `hot.empty` 文案；点击进详情（同卡片交互：详情页跳转 / 首页弹窗）；IntersectionObserver 入场动效、语言切换重译。
- **i18n**：新增 `hot.title/hot.sub/hot.empty` 3 key × 6 语言。
- **`style.css`**：`.hot-strip/.hot-item/.hot-rank/.top1` 样式（渐变描边 TOP1 卡、响应式两列）。
- **冒烟 `[33]` 组**：静态（download/index 两页热门容器、i18n 6 语言、main.js hotItemHTML/loadHot/点击行为、style.css）+ 运行时（造测试产品 A/B → hot 返回数组 ≤10、A 下载 1 次排 B 之前、A downloads=1、清理无残留）。

### 4.26 维度 66 — 下载统计防刷去重（2026-09-10）
- **背景**：用户确认的候选方向「下载统计防刷去重」。下载上报此前不记 IP、同产品重复点击/脚本盗刷会无限膨胀下载量，进而扭曲热门排行与卡片徽标。
- **`server/db.js`**：downloads 表幂等新增 `ip` 列（`ALTER TABLE` 迁移，新库建表直含）+ `(ip, product_id, ts)` 去重索引 `idx_downloads_ip_prod`。
- **`server/routes.js`**：下载上报（`POST /api/downloads`）改为「同 IP 24h 窗口内对同一产品只计 1 次」——命中窗口内既有记录直接返回 200 不入新行；无记录才插入。旧数据不受影响。
- **冒烟 `[34]` 组**：静态（db.js 建表含 ip 列/迁移/去重索引、routes.js 24h 窗口查询）+ 运行时（造测试产品 → 同 IP 上报 3 次 → 该产品下载量=1 → 清理无残留）。

### 4.27 维度 67 — 后台产品图片直传（2026-09-10）
- **背景**：用户确认的候选方向「后台产品图片直传」。此前 `image` 字段只能手填外链或手抄站内路径，后台无法直接上传图片素材。
- **`server/routes.js`**：新增 `POST /api/admin/upload`（authMiddleware + requireAdmin）——body `{data: base64 图片}`；解析 data URL 取 MIME，白名单仅 `image/png|jpeg|webp|gif`（拒 SVG/XSS 载体）；`sniffImage(buf)` 魔数嗅探内容（拒「伪装成 PNG 的 SVG」等伪造文件）；大小 ≤2MB；落盘 `assets/img/products/prod-<ts>-<rand>.<ext>`，返回 `{ok:true, url:'/assets/img/products/…'}`。
- **后台 `admin.html` + `admin.js`**：产品表单新增文件选择 `#f-image-file`（accept 限图片）+「⬆ 上传图片」按钮 `#f-image-upload`；FileReader → `readAsDataURL` → POST → 成功自动回填 `image` 输入框并提示；失败 toast。
- **i18n**：新增 `f.upload` 1 key × 6 语言。
- **冒烟 `[35]` 组**：静态（routes.js 路由/白名单/魔数嗅探、admin.html 文件选择与上传按钮、admin.js FileReader 上传逻辑、i18n 6 语言）+ 运行时（上传 1x1 PNG → ok+url+落盘后清理、拒绝 SVG、拒绝伪装 PNG、拒绝 text/plain）。

### 4.28 维度 68 — 审计/留言数据导出（2026-09-10）
- **背景**：用户确认的候选方向「审计/留言数据导出」。留言/审计/产品数据此前只能在后台看板内翻页查看，无法导出归档或线下分析。
- **后台 `admin.html`**：留言面板「⬇ 导出 CSV」（`#export-csv`）、产品表格「⬇ 导出 CSV」（`#export-products`，`data-admin-only` 仅管理员可见）、审计面板「⬇ 导出审计 CSV」（`#export-audit`）。
- **后台 `admin.js`**：通用 `fetchAllPages(url, params)` 跨页全量拉取（pageSize=100 逐页直至拉完，401 自动登出）；`csvCell(v)` 字段转义（含逗号/引号/换行加引号包裹）；`exportCsv`（留言，联动 status/keyword 筛选）、`exportProducts`（产品，内存全量无需请求）、`exportAudit`（审计，联动 action/keyword 筛选）三个导出函数；CSV 带 BOM（`\uFEFF`）+ CRLF，Blob 下载自动命名 `绿角犀留言_YYYYMMDD-HHmm.csv` 等。
- **i18n**：新增 `btn.exportAudit` 1 key × 6 语言（`btn.exportCsv` 复用既有键）。
- **冒烟 `[36]` 组**：静态（admin.html 三按钮、admin.js fetchAllPages/三导出函数/csvCell 转义/BOM+CRLF/留言与审计联动筛选、i18n 6 语言）+ 运行时（审计分页 pageSize=100 返回 rows、留言分页 pageSize=100 且 status=new 全部命中）。

### 4.29 维度 69 — 后台批量导入产品（CSV）（2026-09-10）
- **背景**：用户确认的新候选方向（G1，推荐优先）。此前产品只能后台逐个上架，运营批量迁移 / 与导出闭环（维度 68）对应的批量录入缺失。
- **服务端 `routes.js`**：`POST /admin/products/import`（admin）：`parseCsv(text)` RFC4180 解析（双引号包裹字段、`""` 转义、逗号/CRLF/LF、自动去 UTF-8 BOM、丢弃全空行）；表头中英文别名（`name/名称`、`type/类型`、`category/分类`、`desc/简介/描述`、`icon/图标`、`image/产品图/图片`、`download_url/下载链接/下载地址`、`version/版本`、`changelog/更新日志`、`sort/排序权重`）；类型列支持中英文映射（`应用→app`、`游戏→game`）。
- **数据一致性**：逐行经既有 `normalizeProduct` 校验（与手动上架同规则），**任一行非法 → 整批 400 拒绝并返回 `{ok:false,total,failed:[{line,error}]}`**（line 为 CSV 行号，含表头偏移）；全部合法 → SQLite 事务写入（`db.transaction`），单次 ≤100 行（`CSV_MAX_ROWS`），写审计 `product_import`（count=）。
- **后台 `admin.html/js`**：「⬆ 导入 CSV」按钮 + 隐藏 `file` 输入（`accept=".csv,text/csv"`）→ `FileReader.readAsText(f,'utf-8')` → POST 后 `showToast` 成功条数 / 失败逐行报错（含行号），成功后刷新列表。
- **i18n**：新增 `btn.importCsv` 1 key × 6 语言。
- **冒烟 `[37]` 组**：静态（路由/parseCsv/上限 100/中英别名/整批拒绝逻辑、admin.html 导入按钮与隐藏 file、admin.js FileReader、i18n 6 语言）+ 运行时（合法 CSV（含引号逗号字段）→ 导入 2 条且公开可见 desc 完整；非法行 → 整批拒绝并报第 3 行；缺 name 表头 → 400；空 CSV → 400；清理导入测试产品无残留）。

### 4.30 维度 70 — 下载页排序切换（2026-09-10）
- **背景**：用户确认的新候选方向（G2）。下载页此前仅按后台排序权重展示，访客无法按「最新 / 下载量 / 名称」重新排列。
- **前端 `download.html` + `main.js`**：筛选区新增排序下拉 `#sort-filter`（`aria-label="排序方式"`，4 档：`default`=后台权重+ID 升序 / `new`=最近上架（created_at 降序）/ `hot`=下载量降序 / `name`=名称 localeCompare）；`state` 扩展 `sort:'default'`；`sortProducts(list)` 四分支实现，`renderProducts` 对 app/game 两桶分别调用排序；`change` 监听重渲染。
- **数据支撑**：公开 `/api/products` 已透出 `created_at` 与 `downloads`（维度 63 起），前端无需新请求。
- **i18n**：新增 `sort.default / sort.new / sort.hot / sort.name` 4 key × 6 语言。
- **冒烟 `[38]` 组**：静态（download.html 下拉含 4 档、main.js state 默认值与 sortProducts 四分支/localeCompare/两桶应用/change 监听、i18n 6 语言）+ 运行时（创建 2 个不同权重/时间/下载量/名称的测试产品，验证公开接口 `created_at`/`downloads` 齐全且四维大小关系明确，清理无残留）。

### 4.31 维度 71 — 前台公告条（后台可维护）（2026-09-10）
- **背景**：用户确认的新候选方向（G3）。站点缺一个全局性的运营通知出口（版本发布、维护预告等）。
- **`db.js`**：新增 `announcements` 表（id / content / enabled 默认 1 / created_at / updated_at）。
- **`routes.js`**：公开 `GET /announcement` 返回最新一条启用中的公告（`WHERE enabled=1 ORDER BY id DESC LIMIT 1`，无则 null）；后台 CRUD：`GET/POST /admin/announcements`、`PUT/DELETE /admin/announcements/:id`（内容非空 + ≤200 字符校验，写审计 `announcement_add/edit/delete`）。
- **前台展示**：`index/download/contact/product/privacy/terms` 6 个页面顶部公告条（`.announcement-bar`，`#ann-bar/#ann-msg/#ann-close`，`hidden` 初始）；`main.js` 与 `product.js`（独立详情页）拉取 `/announcement` 填充，**关闭后 `localStorage`（`gr-ann-off-<id>`）记忆该条不再显示**；`style.css` 补 `.announcement-bar` / `.ann-close` 样式。
- **后台 `admin.html/js`**：「前台公告条」面板（`#ann-add-form` 发布 + `#ann-list` 列表 + `#refresh-announcements` 刷新），支持启停切换与删除（删除走二次确认），登录后 `loadAnnouncements()`。
- **i18n**：新增 `panel.announcements / ann.content / ann.add / ann.empty / ann.delete / ann.delTitle / ann.confirmDel / ann.needContent / ann.added` 9 key × 6 语言。
- **冒烟 `[39]` 组**：静态（db.js 建表、routes.js 公开/后台路由+审计+长度校验、admin.html 面板、admin.js 逻辑、main.js/product.js 公告条、style.css、6 页面容器、i18n 6 语言）+ 运行时（无公告→null；发 2 条→公开返回最新乙；停用乙→回退甲；编辑甲→公开同步新内容；后台列表按 id 倒序；空/超长→400；未登录→401；清理后恢复 null 无残留）。

### 4.32 维度 72 — 详情页相关推荐（同分类优先）（2026-09-10）
- **背景**：用户确认的新候选方向（G4）。详情页底部此前只显示「更多产品」全量列表，无相关性引导，跳失率高。
- **`routes.js`**：公开 `GET /products/:id/related?limit=`（limit 1~8 钳制默认 4）：**同分类优先 → 同类型补充 → 其余兜底**三层查询，均排除自身（`id != ?`），每层按下载量/权重/ID 排序；产品不存在 404、非法 id 400。
- **`product.html/js`**：「更多产品」区标题改「相关推荐」（`data-i18n="pd.related"`）；`renderMore(currentId)` 改调 `/products/:id/related?limit=4`，仍复用卡片渲染与空态处理。
- **i18n**：新增 `pd.related` 1 key × 6 语言。
- **冒烟 `[40]` 组**：静态（related 路由、三层推荐+排除自身正则、limit clamp、product.html 标题与容器、product.js 调 /related、i18n 6 语言）+ 运行时（建 A/B/C 同分类「工具」3 个 + D 异分类「效率」1 个：A 推荐排除自身且同分类优先、limit=1 仅 1 条、limit=99 clamp 到 8、C（game）推荐含同分类 A/B、D 异分类排除自身、不存在产品 404、非法 id 400、清理无残留）。

### 4.33 维度 73 — 站点维护模式（2026-09-11）
- **背景**：用户确认的新候选方向（H1）。站点上线后需在升级/内容维护期间对访客统一返回维护提示，同时保证管理员可随时进后台关闭。
- **`server/server.js`**：维护中间件置于「域名替换 / HTML 服务中间件」**之前**（否则维护中 HTML 会被直接 200 服务，维护页根本到不了浏览器——此坑由冒烟 `[41]` 运行时断言发现）。`maintenanceOn()` 读 `settings.key='maintenance'`；开启时放行：后台页 `/admin.html`、后台接口 `/api/admin/*`、站点配置 `/api/site`、探针 `/api/health*`、静态资源 `/assets/`、PWA/SEO 根级文件（sw.js/manifest/favicon/robots/sitemap/rss）；前台 HTML（`/` 与根层 `*.html`）→ 503 维护页（`maintenance.html` 内注入后台设置的提示语 `__MAINT_MSG__`，无提示语则留空）；公开业务 API → `503 {error:'maintenance'}`。
- **`server/routes.js`**：`GET/PUT /admin/maintenance`（admin 权限）：GET 返回 `{enabled, message}`；PUT 校验 `enabled` 0/1、`message` ≤200 字符，写 `settings` 并审计 `maintenance_on/off`。
- **`maintenance.html`**：暗色维护页，`id="maint-msg"` 占位展示提示语，自动读取当前语言。
- **`admin.html/js`**：新增维护面板（`maint-on`/`maint-off`/`maint-msg` 输入）；`loadMaintenance()` 拉取状态、`setMaintenance(enabled)` 提交并二次确认（确认文案明示「普通访客将无法访问前台」）。
- **i18n**：`panel.maintenance/maint.*` 共 7 key × 6 语言。
- **冒烟 `[41]` 组**：静态（server.js 维护判定+放行+503 分支、routes.js GET/PUT+长度校验+审计、maintenance.html 占位、admin.html 面板、admin.js loadMaintenance/setMaintenance、i18n 6 语言）+ 运行时（初始关闭 → 开启 → 首页/公开 API/未知页面 503 且带提示语 → admin.html/静态资源/health/site/后台 stats 200 放行 → 关闭恢复 200 → 最终状态关闭无脏数据）。

### 4.34 维度 74 — 产品批量管理（2026-09-11）
- **背景**：用户确认的新候选方向（H2）。后台产品逐条操作效率低，批量上下架/改分类/删除是运营刚需。
- **`server/routes.js`**：`POST /admin/products/bulk/status`、`/admin/products/bulk/category`、`/admin/products/bulk/delete`（admin 权限）：`normalizeBulkIds()` 校验 ids 为 1~500 个正整数（空/超长/含非正整数 → 400）；status 仅 on/off、category 须为枚举项且无非法字符；逐条更新/删除，返回 `{ok, changed}`；写审计。
- **`admin.html`**：产品表多选列 + 全选当前筛选结果（`pm-select-all`）+ 批量操作条（`product-batch`：批量分类下拉 `batch-category`、批量改分类 `batch-cat-apply`、批量上架/下架 `batch-shelf-on/off`、删除选中 `batch-del-products`）。
- **`admin.js`**：`bulkProduct(op, payload)` 统一入口分发 status/category/delete；勾选状态跨页保留（`selectedProductIds`），无选中时 toast 提示；批量删除走二次确认弹窗。
- **i18n**：`bulk.*` 共 6 key × 6 语言。
- **冒烟 `[42]` 组**：静态（3 个批量端点、normalizeBulkIds 校验正则、admin.html 全选/批量条/分类下拉、admin.js bulkProduct 分发 status/category/delete、i18n 6 语言）+ 运行时（建临时枚举分类 + 3 个产品：批量下架 changed=3 且均 off → 上架 → 改分类为枚举项 changed=3 且均命中 → 清空分类 changed=3；空 ids/非法 id/非法 status/非法分类字符/非枚举分类/超 500 个 → 400；未登录 401；批量删除 200；分类与产品清理无残留）。

### 4.35 维度 75 — 访问分析可视化（2026-09-11）
- **背景**：用户确认的新候选方向（H3）。此前仅有 PV/UV 与来源维度，缺少浏览器/系统/语言/时区分布的可视化，运营无法判断目标受众环境。
- **`server/routes.js`**：公开 `POST /api/views` 上报 `{page, ua, lang, tz}` 写入 visits（沿用现有 PV 上报，扩展字段）；`parseUa(ua)` 零依赖解析浏览器（Chrome/Edge/Firefox/Safari 等）与系统（Windows/macOS/Android/iOS/Linux 等）；后台 `GET /admin/stats` 单次扫描 `SELECT referer, ua, lang, tz FROM visits` 聚合浏览器/系统/语言/时区四类分布（top N + 其他归并）。
- **`admin.html/js`**：新增「访问分析」面板：浏览器/系统分布条（`browser-bars`/`os-bars`）+ 语言/时区列表；`renderAnalytics()` 消费 `stats.browsers/oses/langs/timezones`。
- **i18n**：`panel.browserDist` 等 4 key × 6 语言。
- **冒烟 `[43]` 组**：静态（parseUa、stats 单次扫描聚合 SQL、响应含 browsers/oses、admin.html 容器、admin.js 渲染、i18n 6 语言）+ 运行时（造 4 条不同 UA 浏览记录：Chrome/Edge/Firefox 与 Windows/macOS/Android 均被正确识别进分布；本地模式直连库清理测试记录、无残留）。

### 4.36 维度 76 — 产品用户反馈（2026-09-11）
- **背景**：用户确认的新候选方向（H4）。官网缺少用户对产品的评价收集通道，后台也无法按产品管理反馈。
- **`server/routes.js`**：公开 `POST /api/feedback`：仅允许**上架**产品（`status='on'`，否则 404）；蜜罐字段 `company`/`website` 非空 → 400；按 IP `rateLimit('feedback')` 防刷；评分 rating 0~5 可缺省、内容 1~500 字、邮箱（可选）格式校验；写 feedbacks 表 + 审计。后台 `GET /admin/feedback`（联表产品名、按 `product_id`/`keyword`/`status` 筛选、分页返回 `{rows,total}`）、`PUT /admin/feedback/:id`（status 仅 new/done）、`DELETE /admin/feedback/:id`（均 admin 权限）。
- **`product.js`**：详情页新增反馈区 `feedbackHtml(p)` + 提交逻辑 `initFeedback(p)`（评分星选 + 内容 + 邮箱 + 蜜罐隐藏字段），提交成功提示并重置。
- **`admin.html/js`**：反馈管理面板（列表 `feedback-list`、产品筛选 `fb-product-filter`、状态 chips `feedback-filter`）；`loadFeedback(opts)`/`renderFeedback()` 渲染，标记已处理/删除。
- **i18n**：`fb.*` 共 7 key × 6 语言。
- **冒烟 `[44]` 组**：静态（公开提交与 3 个后台端点、上架产品校验+蜜罐+限流正则、product.js 反馈区与提交、admin.html 面板、admin.js loadFeedback/renderFeedback、i18n 6 语言）+ 运行时（正常提交评分 5 → ok；无评分 → ok；缺产品 id 400、产品不存在 404、空内容/超 500 字/邮箱格式错/蜜罐命中 → 400；后台列表联表产品名、按产品/关键词筛选命中、标记已处理 + 按状态筛选、非法 status 400、未登录 401、删除反馈 200；清理无残留）。

### 4.37 维度 77 — 前台评分聚合展示（2026-09-11）
- **背景**：用户确认的 H6 新方向（H6a）。反馈仅存于后台，前台详情页看不到已审核评价，无法形成「反馈→评分→口碑」闭环。
- **`server/routes.js`**：公开 `GET /products/:id/feedback`：`status='on'` 校验（否则 404）、非法 id 400；仅统计**已审核（`status='done'`）**反馈——未审核反馈前台天然不可见（审核闭环）；聚合 SQL 区分 `count`（总条数，含无评分）与 `ratedCount`/`avg`（仅计 rating>0，均值保留 1 位小数，无评分 0）；`rows` 取最新评价摘要（时间倒序，`limit` 1~20 钳制缺省 5）。返回 `{count, ratedCount, avg, rows}`。
- **`product.js`**：`loadFeedbackSummary(productId)` 拉取聚合 + `renderFeedbackSummary(d)` 渲染到评价区 `#fb-summary`（`fb-avg` 平均分大数字 + ★ 星标 + 「已获 N 人评分」，下方 `fb-sum-list` 最新评价列表：星标 + 内容 + 称呼 + 时间）；反馈提交成功后刷新摘要。
- **i18n**：`fb.avgBy / fb.recent / fb.anon` 等 3 key × 6 语言。
- **冒烟 `[45]` 组（含 77–80）**：静态（routes.js 聚合端点与 `status='done'`、product.js 两函数与容器、i18n 6 语言）+ 运行时（造 2 条评分反馈未审核 count=0 → 后台标记 done 后 count=2/ratedCount=2/avg=4、rows 含摘要、limit=1 只回 1 条、第 3 条无评分计入 count 不计均值、下架产品 404/非法 id 400、测试数据清理）。

### 4.38 维度 78 — 产品更新动态聚合页 changelog.html（2026-09-11）
- **背景**：用户确认的 H6 新方向（H6b）。version/changelog 散落在各产品详情页，访客无法一页纵览全系更新动态。
- **`changelog.html`**：沿用全站导航/页脚/子页头结构（hero 区 `chg.title/chg.sub` + 列表容器 `#chg-list`），底部引用 `changelog.js`；6 个前台页面导航新增「更新动态」入口（`nav.changelog`）。
- **`assets/js/changelog.js`**：拉取公开 `GET /api/products`，**仅展示 `version` 或 `changelog` 非空**的产品（过滤无更新项），按产品分组卡片（`.chg-card`：图标 + 产品名链接进详情 + `v版本` 徽标 + 类型标签 + `chg-logs` 日志行列表，无日志显示「暂无更新说明」），`observeReveals()` 入场动画；空态/加载失败提示。
- **`sw.js`**：缓存版本升 `lvjx-v4`，预缓存 `changelog.html` 与 `assets/js/changelog.js`（离线可用）。
- **i18n**：`nav.changelog / chg.*` 共 6 key × 6 语言。
- **冒烟 `[45]` 组**：静态（changelog.html 含容器与脚本引用、changelog.js 拉取 `/products` + filter + `class="chg-logs"`、导航入口、sw.js 预缓存）+ 运行时（`/api/products` 返回 version/changelog 字段）。

### 4.39 维度 79 — 后台反馈/访问分析数据导出（CSV）（2026-09-11）
- **背景**：用户确认的 H6 新方向（H6c）。反馈与访问分析数据后台只能逐页查看，无法导出离线分析或归档。
- **`admin.html`**：反馈面板新增「导出反馈 CSV」（`#export-feedback`）、访问分析面板新增「导出分析 CSV」（`#export-stats`）。
- **`admin.js`**：`downloadCsv(prefix, rows)` 生成带 **BOM + CRLF + 字段转义**的 CSV（Excel 打开不乱码，UTF-8 可识别）；反馈导出**联动当前筛选**（status/keyword/product_id）+ 复用 `fetchAllPages`（pageSize=100 跨页全量拉取），列：提交时间/产品/称呼/邮箱/评分/反馈内容/状态；分析导出聚合 `lastStats` 多区块（流量来源/引荐域名/访问语言/访问时区/浏览器分布/系统分布/热门产品）。
- **i18n**：`panel.exportFeedback` 等 1 key × 6 语言。
- **冒烟 `[45]` 组**：静态（admin.html 两按钮、admin.js `downloadCsv` + `fbExportBtn` + `statsExportBtn`）。

### 4.40 维度 80 — 后台反馈概览卡片（2026-09-11）
- **背景**：用户确认的 H6 新方向（H6d）。后台看板无反馈总量/健康度视图，运营无法一眼掌握反馈水位与产品分布。
- **`server/routes.js`**：`/admin/stats` 响应扩展 `feedback: {total, pending, ratedCount, avg, byProduct, week}`——总数/未处理数/有评分数/平均分（仅计 rating>0）；`byProduct` 联表产品名 GROUP BY 按条数降序 TOP5（已删产品显示「#已删除产品」）；`week` 近 7 天趋势按本地时区 `strftime('%Y-%m-%d', ts/1000, 'unixepoch', 'localtime')` 分组 + `todayStart()` 补零。
- **`admin.html/js`**：看板新增「反馈概览」面板：KPI 网格 `#fb-overview-kpis`（总数/未处理（>0 高亮）/有评分数/平均分）+ 按产品分布条 + 近 7 天趋势条；`applyStats` 消费 `stats.feedback` 渲染。
- **i18n**：`panel.fbOverview / kpi.fb* / fb.byProduct / fb.week7` 共 6 key × 6 语言。
- **冒烟 `[45]` 组**：静态（routes.js stats 含 `feedback/byProduct/week`、admin.html `#fb-overview`、admin.js 渲染）+ 运行时（stats.feedback 字段类型正确、byProduct 数组、week 恰 7 天）。

---

## 5. 回归自检（核心护栏）

**务必在每次改动（尤其动到 `server.js` 静态托管/中间件或前端）后运行：**

```bash
npm run smoke            # 探测 3000；没跑则临时拉起随机端口实例，结束自动关
npm run smoke:spawn      # 强制临时实例，不干扰开发服务（CI 用）
node tools/smoke-test.js --url=https://域名   # 线上巡检
```

- 零依赖（仅 Node 内置），**失败退出码 1**，可作 CI 门禁 / 发布前检查。
- 架构：每个维度一个 `section('[N] ...')` + `ok(name, cond, detail)`；HTTP 走 `req()/status()`；**纯前端不变量（a11y/SEO 标记）直接读仓库文件做正则断言**，不依赖运行实例。
- 当前 **749 项 / 45 组**（本地；2026-09-12 生产 `--url` 实测 **746 项全绿**）：页面可用性 / API 契约 / 后台鉴权 / 静态白名单 / 资源缓存 / 安全头 / SEO / 留言防滥用 / 可观测性 / a11y / 结构化数据 / 后台二次确认 / 产品表单双重校验 / 错误页增强与图片渲染 / prefers-reduced-motion 降级 / 404 搜索键盘导航与高亮 / 留言批量操作 / 维度索引文件 / 留言服务端分页与搜索筛选 / 404 搜索模糊匹配 / 管理员角色编辑与账号删除 / PWA 新版本更新提示 / 产品置顶/排序权重 / 流量趋势（可切换天数） / 审计/访问日志服务端分页 / 产品分类后台维护 / 产品大图/缩略图 / 全站 img 响应式（srcset/sizes）/ 合规基础页 / 产品更新日志 / 下载量展示+热门 / 导航栏全站搜索 / 热门下载排行 / 下载统计防刷去重 / 后台产品图片直传 / 审计/留言数据导出 / 后台批量导入产品（CSV）/ 下载页排序切换 / 前台公告条 / 详情页相关推荐 / 站点维护模式 / 产品批量管理 / 访问分析可视化 / 产品用户反馈 / H6 评分聚合与动态页。

---

## 6. 运行 / 测试 / 部署

```bash
npm install                       # express + better-sqlite3
node server/server.js             # 默认 3000；PORT=8080 可改
# 后台默认口令 admin123；改 server/config.js 的 adminPassword 或 env ADMIN_PASSWORD
```

- 容器：`docker build -t lvjiaoxi-website . && docker run -d -p 3000:3000 -v $(pwd)/data:/app/data lvjiaoxi-website`
- 运维：`npm run backup` / `npm run maintain` / `node tools/maintain.js verify [--all]` / `restore --file=...`
- 完整部署（PM2 / systemd / nginx / HTTPS / 备份演练 / 升级）：见 **`DEPLOY.md`**

### 6.1 生产环境（阿里云，2026-09-12 已上线）

- 服务器：`47.116.59.141`（root；SSH 密钥在本机 `C:\Users\Administrator\.ssh\greenrhino_deploy`）
- 代码目录：`/opt/lvjiaoxi-web`；PM2 进程 `lvjiaoxi-web`（fork 单实例，端口 3002，`max_memory_restart: 256M`）
- 启动命令（已在跑，勿重复 start；日常操作用 `pm2 restart lvjiaoxi-web`）：

  ```bash
  NODE_ENV=production PORT=3002 SITE_URL=https://lvjiaoxi.com \
  ADMIN_PASSWORD=<生产口令> AUTH_SECRET=<生产密钥> \
  pm2 start server/server.js --name lvjiaoxi-web
  ```

- **启动陷阱**：勿用 `pm2 start ecosystem.prod.js` —— PM2 只自动识别名为 `ecosystem.config.js` 的配置文件，其它文件名会被当普通脚本执行（进程显示 online 但端口不监听，已踩坑，见第 7 节第 9 条）
- 环境变量已注入 PM2 env；`pm2 save` + `pm2 startup systemd` 已配开机自启。仓库内 `deploy/` 模板为 `__CHANGE_ME__` 占位，需替换真实值
- 健康检查：`curl -s http://127.0.0.1:3002/api/health` → `{"status":"ok",...,"db":"ok"}`
- nginx：`/etc/nginx/conf.d/lvjiaoxi.conf`（`lvjiaoxi.com` / `www.lvjiaoxi.com` → `127.0.0.1:3002`；当前 HTTP 临时版，HTTPS 待 DNS 生效后 certbot 升级）

---

## 7. 关键约定与坑（必读，否则必踩）

1. **登录接口契约 = 用户名 + 口令**：`POST /api/admin/login` 必须 `{username, password}`，默认 `username:'admin'`。冒烟用例缺 `username` 曾致 `--spawn` 长期假性 401。
2. **域名单一来源**：HTML 写占位 `https://lvjiaoxi.example.com`，由 `server/server.js` 中间件在响应时替换为 `config.siteUrl`（env `SITE_URL`）。结构化数据/OG/canonical 里写占位即可，会被自动替换。
3. **`--spawn` 与运行实例共用 `data/app.db`**：只读无碍；不要在临时实例写库后依赖运行实例看到。
4. **Windows 路径**：给 node 传路径用正斜杠 `C:/Users/...`，反斜杠会被 Git Bash 吞。
5. **静态托管白名单守卫**挂在 `express.static` 前：db/auth/server/节点模块/.env/backups 一律 404。
6. **CSP `style-src` 保留 `'unsafe-inline'` 是刻意取舍**：页面有 31 处 `style=""` 且 `admin.js` 等经 CSSOM 动态设样式，移除会破坏后台与交互。
7. **i18n 长正文保持中文**：仅框架文案（导航/页脚/按钮/后台面板）多语言，产品简介等正文不翻译。
8. **本地 `.env` 不要设 `ADMIN_PASSWORD`**：冒烟 `tools/smoke-test.js` 不加载 `server/env.js`（其登录口令直读 `process.env.ADMIN_PASSWORD` 兜底 `admin123`），而 server 启动会加载 `.env`——一旦 `.env` 设置 `ADMIN_PASSWORD`，本地冒烟会登录失败（749 项全挂）。生产口令经 PM2/systemd 环境变量注入。`.env` 可放心放 `AUTH_SECRET`（冒烟不依赖）。
9. **PM2 配置文件命名陷阱（2026-09-12 生产踩坑）**：PM2 只把名为 `ecosystem.config.js` 的文件当配置加载；`pm2 start ecosystem.prod.js` 会把配置本身当 node 脚本执行（进程命令变 `node .../ecosystem.prod.js`），表现为进程 online、日志为空、端口不监听。生产已改用显式命令 `pm2 start server/server.js --name lvjiaoxi-web` 并注入 env（见 6.1 节）。
10. **生产巡检必须带真实口令**：`tools/smoke-test.js` 登录口令读 `process.env.ADMIN_PASSWORD`（兜底 `admin123`）；对生产 `--url` 巡检不带 `ADMIN_PASSWORD=<生产口令>` 会全部鉴权断言 401 失败。
11. **生产 PM2 env 注入方式（2026-09-13 踩坑）**：PM2 `ecosystem.config.js` 的 `env` 对象在 cluster 模式下不注入到 worker 进程的 `process.env`；改为 `exec_mode: 'fork'` 才生效。当前生产同时用 **fork 模式 + 项目根 `.env` 文件** 双保险（env.js 加载 `.env`，不覆盖已存在变量）。之前服务器 `ADMIN_PASSWORD` 登录失败是因为 `curl` 时漏了 `username` 字段（登录接口契约必须 `{username:'admin', password:'xxx'}`）。
12. **数据库 admin 用户口令是首次启动时的快照**：`ensureAdmins()` 只在 admins 表为空时用 `config.adminPassword` 种种子。如果先启动（用 admin123 种了 admin），后来才设 `ADMIN_PASSWORD` 环境变量，数据库里的口令还是 admin123 的 hash。解决：用 `node -e "const db=require('./server/db'); const crypto=require('crypto'); const salt=crypto.randomBytes(16).toString('hex'); const h=crypto.scryptSync('新口令',salt,32).toString('hex'); db.prepare('UPDATE admins SET pass_hash=?, salt=?, pwd_version=pwd_version+1 WHERE username=?').run(h,salt,'admin')"` 重置。

---

## 8. 待用户提供的外部依赖（上线前置，未动、切勿 mock）

| 缺口 | 说明 | 影响 |
| --- | --- | --- |
| 真实安装包 | 放 `downloads/`，后台 `download_url` 填 `/downloads/xxx` 即生效 | 下载页无真实可下载物（生产当前 `/downloads/` 404） |
| 品牌素材 | Logo、OG 分享图 `assets/img/og.jpg`（1200×630）、配色变量 | 视觉非正式 |
| SMTP 凭据 | 留言通知邮件发送 | 后台留言无邮件提醒 |
| 域名 DNS | `lvjiaoxi.com` / `www` 的 A 记录尚未指向 `47.116.59.141`（需在阿里云域名控制台添加；未生效前 certbot 无法签证书） | 域名不可公网访问，仅 IP+Host 头可测 |

---

## 9. 已知风险 / 遗留

- **`data/app.db` 产品数据存在乱码（发现于 2026-09-10，与本次改动无关）**：当前开发库中 id 17–23 的产品 `name`/`desc` 为 GBK 误写的 UTF-8 乱码（`category` 正常，id 16 正常）。影响：404 搜索与下载页对这些产品的**中文关键词**无法命中（`category`/类型仍可命中）。**未擅自处理**（重灌种子会清空留言/审计/统计等数据）。建议：后台逐个重新编辑受影响产品，或确认无保留数据后删除 `data/app.db` 重启重灌种子。
- （其余无未闭环的工程风险项；维度追踪已收敛至根目录 `维度清单.md`）

---

## 10. 下一步可推进的自包含项（候选）

1. 全站 `<img>` 统一接入 `sizes`/`srcset` 响应式图片——**已完成**（维度 60，见 4.20 节；产品大图已接入 480w/960w 双档 + 断点 sizes；仅剩余 logo/hero 等装饰性小图无此必要）。
2. 产品置顶/排序权重——**已完成**（维度 55，见 4.15 节）。
3. PWA 更新提示浏览器端实测——**已完成**（维度 54，见 4.14 节）。
4. 流量趋势（可切换天数）——**已完成**（维度 56，见 4.16 节；选择依据：候选耗尽后由用户拍板确定）。
5. 产品大图/缩略图：products 表新增 image 字段，下载页/首页卡片展示产品图（可连带激活候选 1 的 srcset/sizes 响应式），需生成或上传产品图素材。——**已完成**（维度 59，见 4.19 节；8 张种子产品图已生成，后续新增产品由后台录入 `image` 字段即可；候选 1 的 srcset/sizes 仍待评估）。
6. 产品分类后台维护：分类从自由输入改为后台可维护枚举，下载页分类区自动生成（现状：datalist 联想 + 自由输入）。——**已完成**（维度 58，见 4.18 节）。
7. 审计/访问日志服务端分页：对标留言已实现的分页/搜索/筛选，防日志增长后后台全量拉取卡顿（现状：日志接口一次全量返回）。——**已完成**（维度 57，见 4.17 节）。
8. 合规基础页（隐私政策 / 服务条款 / robots / sitemap）——**已完成**（维度 61，见 4.21 节；用户确认的候选方向）。
9. 产品更新日志（version / changelog 字段 + 后台维护 + 详情页徽标/日志区）——**已完成**（维度 62，见 4.22 节；用户确认的候选方向）。
10. 下载量展示+热门（公开接口透出 downloads + 卡片/详情徽标 + product_id 索引）——**已完成**（维度 63，见 4.23 节；用户确认的候选方向）。
11. 导航栏全站搜索（9 页 nav-search 表单 + download.html?q= 联动）——**已完成**（维度 64，见 4.24 节；用户确认的候选方向）。
12. 热门下载排行专区（公开 /api/products/hot + 首页/下载页热门区 TOP 榜单）——**已完成**（维度 65，见 4.25 节；用户确认的候选方向）。
13. 下载统计防刷去重（downloads 记 IP + 同 IP 24h 窗口去重，防盗刷膨胀）——**已完成**（维度 66，见 4.26 节；用户确认的候选方向）。
14. 后台产品图片直传（/api/admin/upload：魔数嗅探 + 类型/大小白名单 → 落盘站内目录）——**已完成**（维度 67，见 4.27 节；用户确认的候选方向）。
15. 审计/留言数据导出（后台三键 CSV：留言/审计联动筛选跨页全量 + 产品全量）——**已完成**（维度 68，见 4.28 节；用户确认的候选方向）。
16. 后台批量导入产品（CSV 导入，与现有导出闭环）——**已完成**（维度 69，见 4.29 节；用户确认的新候选方向 G1）。
17. 下载页排序切换（默认/最新/热门/名称）——**已完成**（维度 70，见 4.30 节；用户确认的新候选方向 G2）。
18. 前台公告条（后台可维护 + 前台顶部横幅 + 关闭记忆）——**已完成**（维度 71，见 4.31 节；用户确认的新候选方向 G3）。
19. 详情页相关推荐（同分类优先 → 同类型补充 → 兜底）——**已完成**（维度 72，见 4.32 节；用户确认的新候选方向 G4）。
20. 站点维护模式（后台开关 + 前台 503 维护页 + 审计，后台/静态/探针放行）——**已完成**（维度 73，见 4.33 节；用户确认的新候选方向 H1）。
21. 产品批量管理（多选批量上/下架/改分类/删除，ids 1~500 校验）——**已完成**（维度 74，见 4.34 节；用户确认的新候选方向 H2）。
22. 访问分析可视化（浏览器/系统/语言/时区分布面板，parseUa 零依赖解析）——**已完成**（维度 75，见 4.35 节；用户确认的新候选方向 H3）。
23. 产品用户反馈（详情页提交 + 后台按产品管理，蜜罐/限流/上架产品校验）——**已完成**（维度 76，见 4.36 节；用户确认的新候选方向 H4）。
24. 前台评分聚合展示（详情页平均分 + 有评分数 + 最新评价摘要，后台审核 done 后前台可见）——**已完成**（维度 77，见 4.37 节；用户确认的新候选方向 H6a）。
25. 产品更新动态聚合页 changelog.html（按产品分组展示 version/changelog，导航入口 + SW 预缓存）——**已完成**（维度 78，见 4.38 节；用户确认的新候选方向 H6b）。
26. 后台反馈/访问分析 CSV 导出（反馈联动筛选跨页全量 + 分析多区块聚合，BOM+CRLF 防乱码）——**已完成**（维度 79，见 4.39 节；用户确认的新候选方向 H6c）。
27. 后台反馈概览卡片（总数/未处理/有评分数/平均分 + 按产品分布 + 近 7 天趋势）——**已完成**（维度 80，见 4.40 节；用户确认的新候选方向 H6d）。

---

## 11. 上线必改清单（Checklist）

> 2026-09-12 更新（Nginx 调优 + ICP 备案临时绕过）：**主体部署已完成**（PM2 + nginx 反代，HTTP 临时版）。生产口令与 AUTH_SECRET 已注入 PM2 env（取值见服务器 `pm2 env lvjiaoxi-web`，**未入库，勿外传 / 勿提交 git**）。阿里云 WAF 因 lvjiaoxi.com 未备案拦截域名请求（403 "Non-compliance ICP Filing"），已通过 Nginx 配置让 **IP 直访**（http://47.116.59.141）也能到 lvjiaoxi-web，备案完成后再切回域名。

已完成：
- [x] PM2 部署：进程 `lvjiaoxi-web`（端口 3002），`pm2 save` + `pm2 startup systemd` 开机自启
- [x] `SITE_URL=https://lvjiaoxi.com` 注入 PM2 env（sitemap/OG/canonical/JSON-LD 域名自动替换）
- [x] `AUTH_SECRET` / `ADMIN_PASSWORD` 注入 PM2 env（生产口令 ≠ 默认 admin123）
- [x] nginx 反代：`/etc/nginx/conf.d/lvjiaoxi.conf`（`listen 80 default_server`，server_name 含 `lvjiaoxi.com www.lvjiaoxi.com 47.116.59.141` → `127.0.0.1:3002`）
- [x] **Nginx 冲突修复**：`greenrhino-cloud.conf` 去掉了 `listen 80 default_server` 和 `47.116.59.141` 精确匹配（原配置抢 IP 请求导致 404），备份见 `.bak.keep-ip`
- [x] 生产巡检：`ADMIN_PASSWORD=<生产口令> node tools/smoke-test.js --url=http://127.0.0.1:3002` → **746 项全绿**
- [x] **IP 临时访问**：外网 `http://47.116.59.141` 直接返回 200 + 完整页面

待办：
- [ ] **ICP 备案**（P0）：阿里云控制台提交 lvjiaoxi.com 备案，通过前域名会被阿里云 WAF 拦截（返回 403 "Non-compliance ICP Filing"，Server: Beaver）。备案期间用 IP 直访 http://47.116.59.141 过渡
- [ ] **DNS**：阿里云域名控制台添加 A 记录 `lvjiaoxi.com` / `www` → `47.116.59.141`（备案通过 + DNS 生效后域名访问自动恢复）
- [ ] **HTTPS**：DNS 生效后 `certbot --nginx -d lvjiaoxi.com -d www.lvjiaoxi.com`（模板见 `deploy/nginx.conf`，证书路径已对齐；certbot 会自动把 lvjiaoxi.conf 升级为 HTTPS + 80→443 跳转）
- [ ] **Nginx 回滚**：备案完成后，从 lvjiaoxi.conf 的 server_name 里去掉 `47.116.59.141`，给 greenrhino-cloud.conf 恢复 `default_server` 和 `47.116.59.141`（备份 `.bak.keep-ip` 里有原版），`nginx -t && nginx -s reload`
- [ ] 投放真实安装包至 `downloads/`，后台上架（当前 `/downloads/` 404）
- [ ] 替换品牌素材（Logo / `og.jpg` / 配色变量）
- [ ] 配置 SMTP，验证留言通知
- [ ] HTTPS 落地后跑 `node tools/smoke-test.js --url=https://lvjiaoxi.com` 复检（断言动态读服务端 SITE_URL，不误报）
- [ ] 定期演练 `npm run maintain verify` / `restore`（备份不等于能恢复）

---

## 12. 生产环境速查（新接手先看这一页）

```bash
# 登录（SSH 密钥在本机，直接可用）
ssh -i C:\Users\Administrator\.ssh\greenrhino_deploy root@47.116.59.141

# 状态三连
pm2 list                                   # 看 lvjiaoxi-web 是否 online
curl -s http://127.0.0.1:3002/api/health   # {"status":"ok",...,"db":"ok"} 即正常
ss -tlnp | grep 3002                       # 确认端口监听

# 看日志
pm2 logs lvjiaoxi-web --lines 100          # 业务 / 错误日志
journalctl -u pm2-root -n 50               # 开机自启服务日志

# 生产巡检（口令：cat /opt/lvjiaoxi-web/ecosystem.prod.js 或 pm2 env lvjiaoxi-web 读取）
cd /opt/lvjiaoxi-web && ADMIN_PASSWORD=<生产口令> node tools/smoke-test.js --url=http://127.0.0.1:3002

# 发布更新流程（以 server/routes.js 为例）
scp -i C:\Users\Administrator\.ssh\greenrhino_deploy server/routes.js root@47.116.59.141:/opt/lvjiaoxi-web/server/routes.js
ssh -i C:\Users\Administrator\.ssh\greenrhino_deploy root@47.116.59.141 "pm2 restart lvjiaoxi-web && sleep 3 && curl -s http://127.0.0.1:3002/api/health"

# 数据与备份
ls /opt/lvjiaoxi-web/data/backups/         # 每日自动备份，约保留 7 份
node tools/maintain.js verify --all        # 备份完整性校验（在生产目录内执行）

# 关键路径 / 凭据
# 代码: /opt/lvjiaoxi-web   数据库: /opt/lvjiaoxi-web/data/app.db
# nginx: /etc/nginx/conf.d/lvjiaoxi.conf（改完 nginx -t && nginx -s reload）
# 凭据: /opt/lvjiaoxi-web/ecosystem.prod.js + pm2 env lvjiaoxi-web（未入库，勿外传 / 勿提交 git）
```
